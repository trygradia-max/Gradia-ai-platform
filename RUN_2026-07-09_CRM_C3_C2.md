# Run queue — CRM C3 (service menu + quotes) then C2 (pipeline UI)

_Work queue for Claude Code. Same discipline as OVERNIGHT_RUN_2026-07-08.md: top to bottom, one item at a time, suite + lint + tsc green after EVERY item, log to OVERNIGHT_REPORT.md (append a new dated section), STOP at queue end._

## Hard rails (unchanged + additions)
- Branch `redesign/glass-box` only. No merges, no main/PR actions, no prod/DB actions.
- Spec of record: `_docs/GRADIA_CRM_FOUNDATION_SPEC.md` — **decisions #2–#4 are now RESOLVED in the spec (2026-07-08): 5-page IA approved; quote Book Now inherits the shop's existing booking rule (no new setting).** Build to those.
- **Read `platform/docs/BUILD_REFERENCE.md` before touching any screen.** UX rules from `_docs/GRADIA_UX_ONBOARDING_SPEC.md` hold: every owner card has an owner-clickable action; no vendor names; plain-English labels; the glass-box visual language from the redesign is the aesthetic — extend it, don't invent a new one.
- Pricing numbers come ONLY through `src/lib/service-pricing.ts`. The voice/quote price-identity eval must keep passing.
- Quotes created by the agent are ALWAYS `draft` (staged). Booking from the quote page routes through the existing booking rule + `pending_actions`. Extend locking tests, never weaken.
- Pre-migration tolerance pattern from the C1 code phase applies to all new writes.
- No C4–C8 work. No gated features.

## Queue

### 1. C3a — service menu editor (Settings)
Per spec §C3. Settings page for the service catalog: per-service base price by size class, duration, condition multipliers, add-on/mobile flags, active toggle. Prefill from the existing detailer template menu (onboarding wizard step 1 — reuse, don't duplicate). This page feeds CRM quotes, Agent phone answers, and Whisper grounding — one source of truth. Tests: menu edits change resolvePrice output (fixture-driven).

### 2. C3b — quote builder + branded public quote page
Per spec §C3. Builder reachable from Customers surfaces and (later) pipeline cards: customer+vehicle picker (inline create), service tiles with size-class prices pre-resolved, condition chips, add-ons, photos/notes, total. Target ≤60s to send. Public page `/q/[public_token]`: line items, photos, validity, shop identity, Book Now (inherits booking rule), first view sets `viewed_at`. Send via existing safe_send SMS/email paths (A2P-gated, email fallback). Quote accept/decline updates status + timeline. Agent tool: stage draft quotes from call details (vapi-tools + runtime, staged like every money action).

### 3. C2 — pipeline UI (Customers page, Pipeline tab) + 5-page IA
Per spec §C2 and the resolved IA: Customers becomes the hub with Pipeline (default) / Customers / Quotes tabs. 6-column kanban, colored headers with count + $ total, card face per spec (name · vehicle · interest chip · quote $ · stage-age amber/red · source icon · ⚡ staged-suggestion marker). Drag = stage change writing stage_history; mobile = stage-grouped list with a Stage button. New Lead modal: 3 fields, ≤10s. Right slide-over card: contact header, vehicle chip, quote link, timeline, next action. Auto-moves wired to real events in code (agent lead → new; quote sent → quote_sent; follow-up timer → follow_up; booking approved → booked + lifecycle flip; Lost requires reason). Table-view toggle. Old customer-list URLs redirect (follow the layer-2-shell redirect pattern).

### 4. Wrap
Full suite + lint + tsc. Append run section to OVERNIGHT_REPORT.md with founder actions (expected: apply any new migration, visual review of the three new surfaces, smoke a real quote send on the test shop). Commit + push to `redesign/glass-box`. STOP.
