# Run queue — CRM C6 (Whisper surfaces) then C8 (Today money rows) — FINAL CRM queue

_Work queue for Claude Code. Same discipline as the prior three runs: top to bottom, gates green after every item, dated section in OVERNIGHT_REPORT.md, STOP at queue end. This completes the CRM foundation (C1–C8)._

## Hard rails (unchanged + additions)
- Branch `redesign/glass-box` only. No merges/main/PR/prod actions.
- Spec: `_docs/GRADIA_CRM_FOUNDATION_SPEC.md` §C6/§C8. BUILD_REFERENCE.md before any screen.
- **Eval fixtures FIRST for C6** (P0 discipline): golden suggestion sets over seeded CRM states BEFORE the surface ships. Suggestions may only cite facts queryable from the DB — the candidate picking is SQL, the model only writes copy + the why. No agent loops (locked principle #1).
- Whisper runs are METERED (`usage_events`, credit pre-check, fail closed) — CRM reads/writes are never metered.
- Nothing customer-visible sends without approval unless that automation is autopilot. Every Whisper action lands on the timeline attributed.
- Today-page ordering from GRADIA_UX_ONBOARDING_SPEC.md holds: nudges/suggestions at TOP, revenue cards render only when nonzero. Do not displace the existing ROI receipt.
- Attribution math in SQL from automation_runs→booking joins. No estimates, no model-generated numbers. (ROI-receipt "under-claim" discipline applies.)

## Queue

### 1. Small fix carried from C4 — structured working hours
Settings: per-day working hours (open/close, closed days) on the shop, feeding BOTH the calendar over-capacity warning (replacing the hardcoded 8h/day default flagged in the C4 report) and the booking-rules surface the agent reads. Additive migration if needed. Tests: capacity warning respects configured hours.

### 2. C6a — suggestion queue on Today
Per spec §C6. The "What I'd tackle next" slot becomes Whisper's staged-suggestion queue: each card = plain-English why (grounded: stage age, viewed_at, history) → draft → [Approve] [Edit] [Dismiss]. Candidate sets in SQL: stale quotes (>5d), follow-ups due (next_action_at past), revival-eligible leads (21d+ engaged-then-silent). Single-turn Haiku workers write copy; approve routes through pending_actions/safe_send; dismissals recorded as feedback rows. Eval fixtures first (golden candidates + persona-adherent drafts).

### 3. C6b — draft-anywhere + customer summary
Every reply box gets "Whisper draft" (persona.ts-composed — single persona source, voice/chat/CRM never drift). Customer profile gets one-tap summary: single-turn worker over interactions + vehicles + quotes ("3 jobs, $1,840 LTV, coating Aug 2025, prefers text"). Both metered with pre-check. Tests: summary only states DB-derivable facts (fixture spot-checks).

### 4. C8 — Today money + leak rows
Per spec §C8, BELOW the existing nudges/receipt (do not reorder the page): schedule strip (today's jobs, tap → job card) · money row (revenue booked this week / completed this week / pipeline value by stage / quotes outstanding $) · leak row (new leads today+week / lost this week + top reason / review requests pending). Attribution line on automation run history ("follow-ups booked $X this month") from automation_runs→booking SQL joins. Nonzero-render rule applies to every card. Mobile: single column.

### 5. Wrap
Full gates. Report section: founder actions (visual pass on Today + Whisper surfaces; one end-to-end suggestion approve on the test shop; note any new migration). Update `_docs/GRADIA_CRM_FOUNDATION_SPEC.md` STATUS line: C1–C8 code-complete, pending founder migrations/merge/smokes. Commit + push. STOP.
