# Risk Register

_Created 2026-07-25 by the Organizer. Grounded in the 2026-07-20 technical audit (`platform/docs/audit/`), the decision log (`../11-decision-log.md`), and the P0 ticket set (`../tickets/`). Severity scale defined in `../runbooks/incident-severity.md`. Owner today is the founder in every row — recorded anyway so rows survive the arrival of a team._

Statuses: **Open** · Mitigating (ticket in flight) · Accepted (documented decision) · Closed.

| ID | Risk | Likelihood | Impact | SEV if realized | Current mitigation | Planned mitigation | Owner | Status |
|---|---|---|---|---|---|---|---|---|
| R-01 | Leaked Supabase DB superuser credential (`.gitignore:46`, in pushed git history) is exploited before rotation — full cross-tenant read/write, RLS bypassed | Medium (public-ish repo history) | Catastrophic | SEV-0 | None — credential is live (audit C-1) | **P0-001** rotate today; history-scrub decision in `program/decision-queue.md`; runbook `exposed-credential.md` | Founder | Open |
| R-02 | Cross-tenant leak via a missed `.eq("shop_id")` on any of ~29 service-role files (DB will not catch it; Slack C-2 is the proof-of-pattern) | Medium | Critical | SEV-0 | Code discipline + review; Slack flag off (D-026) | **P0-011** scoping review + `forShop()` helper design; E01 makes it mechanism | Founder | Open |
| R-03 | Double-booking — no conflict/availability check on any booking path (voice, quote accept, drag, block-time) inverts the core product promise | High at pilot volume (partially reduced 2026-08-11: concurrent-write races and replay duplicates are now blocked in production even with enforcement OFF) | High | SEV-1 | **P0-003 (PR #10) + P0-004 (PR #12) + P0-004A (PR #15, 2026-08-11) shipped:** enforcement wired on every booking/reschedule/voice/drag/block-time path (D-015/D-016), and the executor is now atomic + concurrency-safe (per-shop advisory-lock serialization, `pending_action_id` idempotency, persistence-first ordering — **locking + idempotency active in production NOW**, independent of the flag; Cursor APPROVE, real-Postgres concurrency tests green). Conflict enforcement itself stays **dormant in production** (`NEXT_PUBLIC_GRADIA_CONFLICT_ENFORCEMENT` OFF), so slot-overlap refusal is not yet live | **Founder** (last remaining gate — the P0-004A hardening gate is satisfied): manual acceptance (P0-004 steps 1–7) on a flag-on Preview, then Production variable `true` + redeploy (the release event). Risk closes on that evidence | Founder | Open |
| R-04 | Duplicate outbound messaging / duplicate approval cards from normal provider webhook retries (no inbound idempotency on SMS/email) | High (retries are normal provider behavior) | High | SEV-1 | None on inbound SMS/email | **P0-005** idempotency foundation + **P0-006** Twilio MessageSid dedupe (D-023) | Founder | Open |
| R-05 | Double voice-minute billing — Vapi end-of-call retry re-meters minutes (no `vendor_ref` unique on `usage_events`) | Medium | High (owner-visible money error) | SEV-1 | `call_records` upsert is idempotent; metering is not | **P0-007** replay protection (D-023/D-024) | Founder | Open |
| R-06 | Broken build reaches `main` = production — CI runs unit tests only (no typecheck/lint/build; integration tier quarantined red since 2026-06-04) | High | High | SEV-1 | **P0-002 landed 2026-07-30 (PR #9, reviewed APPROVE):** secret hygiene, typecheck, lint, deterministic tests, production build, and the un-quarantined DB integration tier all block; GitHub requires `ci / checks` + `ci-integration / integration` on `main` | Residual watch items: Supabase CLI pin (2.98.2) upgrades need deliberate re-verification; Actions runtime upgrades deferred to a maintenance ticket; live evals excluded (see R-12/Q-06) | Founder | **Closed** (2026-07-30 — evidence in `../tickets/P0-002-ci-enforcement.md` completion record) |
| R-07 | Silent degradation hides revenue-affecting failures — `.catch(() => null)`, best-effort writes, `monitoring.ts` alerts to console only; nobody is paged | High | Medium–High (compounding) | SEV-2 escalating | Sentry captures errors (no alert routing verified) | **P0-012** alert delivery + `/api/health`; structured-failure standard in `08-security-and-reliability.md` | Founder | Open |
| R-08 | TCPA/compliance exposure: `sendOperatorSms` skips send-policy (owner can text an opted-out customer); leads without a customer link escape cooldown/opt-out keying | Medium | High (regulatory) | SEV-1 | Send-policy fail-closed on all AI paths; STOP ledger; TCPA 18-month gate in code | Founder decision in `program/decision-queue.md` (warn/block operator sends); lead-level opt-out keying in **E07** | Founder | Open |
| R-09 | A2P TrustHub SIDs and Housecall Pro endpoint shapes fail on first real use (both self-declared unverified — `twilio-a2p.ts:11`, `housecallpro.ts` TODO(verify)) | Medium | Medium (feature dead on arrival) | SEV-2 | Honest TODO markers; features degrade, don't crash | Founder live-verification runs (audit doc 12 item 13) — tracked in `program/blocked.md`; HCP now **quarantined** per D-030 (unmarketed, flag off, no new investment) with dependency-review ticket P3-001 → Q-19 | Founder | Open |
| R-10 | Cascade-delete chain `auth.users → shops → everything` irreversibly destroys financial ledgers and consent/compliance history; no soft delete anywhere | Low (requires deletion event) | Critical (unrecoverable) | SEV-0 if triggered | None — schema-level cascade is live | **E10** soft delete + archival; interim rule: never delete an auth user or shop row without a verified backup (`data-restore.md`) | Founder | Open |
| R-11 | Prompt injection steers drafted replies/voice answers — inbound bodies and KB text spliced into prompts with `---` delimiters only; exposure highest exactly where Package-2 autopilot narrows human review | Medium | Medium–High | SEV-1/2 | Structural: forced tool schemas, no send tool in loops, HITL staging, money/calendar floor | **E09** injection hardening + injection eval suite; keep autopilot conservative until then | Founder | Open |
| R-12 | LLM prompt/model regression ships on green CI — live evals (Tier 2/3) gate nothing, violating locked principle #6 | Medium | Medium | SEV-2 | 3-tier harness exists and runs on demand | Eval gating cadence decision (**Q-06** in decision queue) + scheduled eval workflow (`09-testing-strategy.md`) | Founder | Open (unchanged by P0-002, 2026-07-30 — the new CI gates deliberately exclude live-provider/model evals pending Q-06) |
| R-13 | Aurinko outage: (a) classifier failure polarity turns every inbound email into a lead/approval card flood; (b) booking approvals fail closed — no appointment can confirm (hard gate `approvals.ts:686`) | Medium | Medium–High | SEV-2 | Fail-closed is at least safe; flood is contained to approvals inbox | Polarity fix in **E07**; native calendar source of truth (**E02**, D-013) removes the booking dependency; runbooks `ai-provider-outage.md` / `calendar-outage.md` | Founder | Open |
| R-14 | Vendor lock/failure without an exit — LLM has no provider seam (model ids in ~14 modules, grep-verified 2026-07-27), embeddings dimension baked into schema, voice on Vapi-hosted gpt-4o-mini | Low–Medium | Medium | SEV-2 | Voice/telephony/CRM seams exist and are disciplined | AI gateway (`ModelProvider`) in **E01/P1** (D-029/ADR-002); classifications + exit strategies now normalized in `vendors/registry.md` (D-030) | Founder | Open |
| R-15 | Solo-founder key-person/ops risk — one person is incident commander, deployer, and account owner for every vendor; response capacity is one human | High | High (availability of response, not of product) | Amplifies every SEV | Runbooks (this directory), zero-founder-touch principle limits per-shop ops | Document credentials/access recovery; revisit at first hire; WIP limits in `program/` keep change risk low | Founder | Accepted (documented) |
| R-16 | Import corrupts live CRM data during onboarding/trial — a bad mapping, dedupe misfire, or partial commit damages the customer's first real dataset; D-006 makes the import the first-run trust moment, so a corrupted import loses the customer permanently (added 2026-07-27) | Medium (every trial starts with one) | High (trust-fatal per customer; possible data loss) | SEV-1 | Recovery import pipeline stages with review + undo-by-import-job; flag OFF, never live-smoked | **D-022** staging→preview→validation→rollback bar enforced on the E03 structured wizard; live smoke before flag flip (`13-release-strategy.md` §3); response anchor: `../runbooks/broken-import.md` | Founder | Open |

## Review cadence

- **Every sprint boundary** (see `program/current-sprint.md`): re-score likelihood/impact, close rows whose tickets landed, add rows from new audit findings or incidents.
- **Every SEV-0/SEV-1 postmortem** must end by adding or updating a row here (see `runbooks/incident-severity.md`).
- **Before each release**, the Release Reviewer checks open SEV-0-capable rows (currently R-01, R-02, R-10) against the release's blast radius.
