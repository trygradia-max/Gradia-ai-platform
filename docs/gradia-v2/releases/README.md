# Releases

_Created 2026-07-25 by the Organizer. Governed by `../13-release-strategy.md`; dates in `../program/release-calendar.md`._

One record per release: `YYYY-MM-DD-<slug>.md`, written by the Release Reviewer at go/no-go time.

Each record contains: included tickets · migrations applied · flags flipped (before → after) · smokes performed (link GO_LIVE_CHECKLIST / `docs/*-go-live.md` items) · claim changes (WHAT_GRADIA_DOES deltas per D-028) · known issues shipped · rollback plan · go/no-go verdict with reasons.

## Index

_No formal release records yet. First expected record: the 2026-08-07 alpha (P0 exit)._

Release-infrastructure note (Organizer, 2026-07-30): **P0-002 CI enforcement merged to `main` (= production) in PR #9** with a green CI run — evidence in `../tickets/P0-002-ci-enforcement.md` (completion record). This shipped under the standing rule that reviewed P0-ticket batches may reach prod ahead of a formal release; from this date, every release record can rely on the "CI green" gate in `../13-release-strategy.md` being mechanically enforced (`ci / checks` + `ci-integration / integration` required on `main`). Completion/release evidence for P0-002 should cite PR #9 and its CI run.

P0-ticket batch note (docs-close session, 2026-08-14): **P0-006 Twilio inbound replay hardening merged to `main` (= production) in PR #19** (`76847e4`; pre-squash `afb542b` Builder implementation → `89af55c` metering retry-safety fix). GitHub CI ultimately passed: `ci / checks` · `ci-integration / integration` · Vercel · Vercel Preview. Independent Cursor verdict: **APPROVE / safe to merge** — no BLOCKER or HIGH findings remained; no Cursor review-fix commit was required. **Real-provider acceptance completed by the founder pre-merge** on an isolated staging/Preview Twilio path: normal valid inbound delivery; same-`MessageSid` replay with no duplicate effects; STOP consent flow; replay of the same STOP SID; invalid/missing-signature rejection with no provider-event or business side effects. Shipped: the Twilio inbound SMS route (`src/app/api/twilio/sms/route.ts`) now claims `(provider='twilio', event_id=MessageSid)` on the P0-005 `provider_events` receipt strictly after server-side credential/shop resolution + signature verification and strictly before any write or LLM call (ADR-001 C3 — forged requests can never claim or poison a MessageSid); completed replays are 2xx no-ops, concurrent duplicates yield exactly one business execution, failed events remain retryable, completed events cannot reopen, and the stale threshold stays above the route `maxDuration`. The first CI run exposed a real transient PostgREST/Kong failure that P0-006 fixed rather than reran: `recordUsage` now returns `written`/`duplicate`/`failed` (still non-throwing for existing callers), and the Twilio inbound path requires durable classification metering before continuing (`failed` → fail the provider event + 500 → provider retry, before any card staging). One accepted residual (Cursor-recorded, non-blocking): a retry after a genuine metering-write failure may re-run the classifier, since classifier output is not persisted — billing is not doubled and only the successful attempt stages state. **No new migration; no Twilio-specific duplicate table** (provider_events is the receipt); status/A2P callbacks remain P0-008; no flags flipped (none — fix, not feature); no claims changed (D-028); production P0-004 conflict enforcement remains OFF. Close record: `../tickets/P0-006-twilio-inbound-replay-protection.md`.

P0-ticket batch note (docs-close session, 2026-08-13): **P0-005 webhook event idempotency foundation merged to `main` (= production) in PR #17** (`e1dedfb`; pre-squash `dec4c38` Builder implementation → `ec28a5a` ADR documentation/review). Independent Cursor verdict: **APPROVE — no BLOCKER or HIGH code defects** (ADR-001 condition C1). **Production duplicate audit (C7) completed by the founder pre-merge:** read-only queries against production on `usage_events (shop_id, kind, vendor_ref)` (excluding `outreach_draft`/null refs) and `automation_runs (automation_id, trigger_ref)` (excluding `failed`/null refs) — **both returned zero rows**; the C7 production-data merge gate was satisfied before merge. Shipped: durable DB-backed provider-event claiming (`provider_events` with `processing`/`completed`/`failed` lifecycle, concurrency-safe duplicate claims, failed/stale reclaim, service-role-only claim primitive — routes must verify signatures BEFORE claiming, per C3); ledger uniques on `usage_events` + `automation_runs`; owner/authenticated financial-ledger writes removed (SELECT-only RLS, tenant reads preserved). Migrations: `20260812120000_webhook_idempotency.sql` + `20260812130000_ledger_rls_select_only.sql`, with unapplied rollback `supabase/rollbacks/20260812_p0_005_down.sql`. **No provider route wired** (P0-006/007 deliberately not started); no flags flipped (none — fix, not feature); no claims changed (D-028); production P0-004 conflict enforcement remains OFF. **Rollout acceptance:** the staging manual acceptance run (verify `provider_events` + both partial uniques; replay voice metering twice → one row/one decrement; overlapping reminder trigger → one `automation_run`; owner INSERT on `usage_events` denied; billing/ROI pages render) is still outstanding — until it completes, the migrations are merged but not considered fully rollout-accepted. Retention follow-up P0-005A filed at close (C2). Close record: `../tickets/P0-005-webhook-idempotency-foundation.md`.

P0-ticket batch note (docs-close session, 2026-08-11): **P0-004A booking atomicity and concurrency merged to `main` (= production) in PR #15** (`2103943`; pre-squash `522203f` Builder implementation → `31f410a` Cursor review fix). Final `ci / checks` + `ci-integration / integration` **green**, including the real-Postgres concurrency tests; one temporary failure during the run was a transient upstream Supabase/PostgREST test-fixture failure and the rerun completed green. Independent Cursor verdict: **APPROVE** — Cursor found **one BLOCKER** and it was fixed pre-merge: the serialized database overlap refusal was still active with conflict enforcement OFF; the fix added `p_enforce_conflicts` so the RPC's overlap refusal follows `FEATURES.conflictEnforcement`. **Live behavior change at merge (deliberate):** transaction-scoped per-shop advisory locking and `pending_action_id` durable idempotency are active immediately, regardless of the flag — they are correctness properties; appointment persistence now precedes external calendar sync, and external sync failures are reconciliation conditions, not failed bookings. Slot-overlap refusal remains dormant: `NEXT_PUBLIC_GRADIA_CONFLICT_ENFORCEMENT` stays `false`/unset in Production. One migration shipped (`20260811120000_booking_atomicity.sql`: additive column + partial unique index + `write_appointment_serialized` RPC). Per §Standing rules the **production flag flip is still the release event**, now gated only on the founder manual QA (P0-004 steps 1–7 on a flag-on Preview) — the P0-004A hardening gate from the P0-004 NOT READY verdict is satisfied. No claims changed (D-028). Completion record: `../tickets/P0-004A-appointment-booking-atomicity-concurrency.md`.

P0-ticket batch note (docs-close session, 2026-08-11): **P0-004 conflict enforcement merged to `main` (= production) in PR #12** (`3b6d044`), `ci / checks` + `ci-integration / integration` green. Independent Cursor final review: **MERGE VERDICT — APPROVE · PRODUCTION ENABLEMENT VERDICT — NOT READY**; the verdict was supplied to the Founder outside the GitHub PR comment trail (the PR itself carries no Cursor review artifact). Fix rounds on-branch: `d43ce16` (Cursor findings), `c0b66b1` (founder rollout/failure-policy directives). The Founder merged only after green CI and with the dark-rollout conditions satisfied. **Zero live behavior change at merge:** enforcement is dormant because `NEXT_PUBLIC_GRADIA_CONFLICT_ENFORCEMENT` defaults OFF (only the exact value `"true"` enables; build-time inlined, so changes require a redeploy). One additive idempotent migration shipped (`20260806120000_appointments_shop_scheduled_idx.sql`). Per §Standing rules the **production flag flip is the release event** and gets the formal record — per the NOT READY enablement verdict it is gated on founder manual QA (ticket steps 1–7 on a flag-on Preview) **and** the P0-004A atomicity/concurrency hardening (issue #13, next implementation ticket). No claims changed (D-028). Merge/review record: `../tickets/P0-004-conflict-enforcement-booking-paths.md`.

P0-ticket batch note (Organizer, 2026-08-06): **P0-003 central appointment conflict service merged to `main` (= production) in PR #10** (`00091db`), Cursor Reviewer verdict APPROVE, `ci / checks` + `ci-integration / integration` green after the review commit. **Zero user-facing surface**: the service (`src/lib/availability.ts`) is dormant until P0-004 wires it into booking paths behind `FEATURES.conflictEnforcement` — under §Standing rules, that future **flag flip is the release event** and gets the formal record; this note is the merge evidence. No migrations, no flags flipped, no claims changed (D-028: nothing new claimable). Merge/review record: `../tickets/P0-003-central-appointment-conflict-service.md`.
