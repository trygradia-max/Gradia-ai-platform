# Runbook — Production Configuration Audit

_Created 2026-09-01 by the autorun Builder (ticket **PROD-CONFIG-AUDIT**, autorun Batch 1 queue item 1, branch `auto/batch-1`). Docs-only output: no code, no `.env*` edits, no env vars created, **no secret values anywhere in this file — names and presence only.** Re-run instructions in §1 regenerate the same table (deterministic grep, sorted names)._

## 0. What this answers

"What is Gradia actually configured to do in Production, and what does each missing secret turn off?" One table (§2), the findings behind it (§3), and the founder actions + owning tickets (§4). Owner-facing consequence today: every "Coming soon" badge on `/settings` is a **server env var being absent**, presented to the owner as a roadmap promise (`src/app/(dashboard)/settings/page.tsx:129-149` → `src/components/gradia/connection-tile.tsx:50-52`).

## 1. Method (2026-09-01)

- **Enumeration:** every `process.env.<NAME>` and `process.env["NAME"]` under `src/` (`*.ts`, `*.tsx`), deduped and sorted:
  ```
  grep -rnoE 'process\.env(\.[A-Za-z_][A-Za-z0-9_]*|\[["'"'"'][A-Za-z_][A-Za-z0-9_]*["'"'"']\])' src --include='*.ts' --include='*.tsx'
  ```
  Result: **50 distinct names** at audit time, matching the Organizer's 2026-09-01 Explore sweep in the ticket — **53 after P0-012 landed later the same session** (`OPS_ALERT_WEBHOOK_URL`, `OPS_ALERT_SMS_TO`, `OPS_ALERT_SMS_FROM`; rows added below). Dynamic/indirect reads were searched separately (§3.5): one dynamic reader (`envHas(name)` in `src/lib/data/agents.ts:39`, four names, all also read statically elsewhere) and one indirection (`getSupabasePublicConfig()` in `src/lib/env.ts`, consumed by `src/lib/supabase/server.ts:4` and `client.ts:3`). No other `process.env` indirection exists in `src/`.
- **Presence:** the Vercel CLI is **not installed** in the Builder environment (`which vercel` → not found; `vercel whoami` → command not found), so `vercel env ls production` was **not** run and no login was attempted (autorun rule 6). Presence is therefore **UNKNOWN** for every variable except the facts already on record:
  - `GRADIA_DASHBOARD_URL` **PRESENT** — founder-confirmed in Vercel Production 2026-08-28 (P0-010 close record, scope item 9).
  - `VAPI_DEFAULT_SHOP_ID` **ABSENT**, `STRIPE_API_BASE` **ABSENT** — founder-confirmed 2026-08-28 (same record).
  - `STRIPE_PRICE_ID`, `STRIPE_PRICE_VOICE_ADDON`, `STRIPE_PRICE_CREDIT_PACK`, `STRIPE_PRICE_MINUTE_PACK` **ABSENT by design** — Q-22/D-034 guard; checkout proven fail-closed at the P0-010 acceptance; stays absent until P0-013 lands.
  - `NEXT_PUBLIC_GRADIA_CONFLICT_ENFORCEMENT` **not `"true"`** in Production (P0-004/P0-004A close; enforcement OFF) — recorded as ABSENT/not-true.
  - Autorun precondition 4 (`program/autorun.md`) lists `VAPI_API_KEY`, `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `AURINKO_CLIENT_ID`, `AURINKO_CLIENT_SECRET`, `CRON_SECRET`, `VAPI_DEFAULT_SHOP_ID` as founder-to-set. Whether that happened could not be verified from this session → those rows stay UNKNOWN (see §3.6 for the `VAPI_DEFAULT_SHOP_ID` contradiction).
- **Documentation baselines compared:** `docs/env-setup.md` (verified 2026-06-01, pre-V2) and `.env.example`.
- **Classes** (ticket scope item 2): **required** — the app or a P0 invariant fails closed/crashes without it; **required-for-feature** — a product surface is disabled without it (surface + owner copy named); **optional/dev-only** — test seams, `*_API_BASE`, model overrides, platform-provided runtime vars; **deprecated-by-decision** — `HOUSECALLPRO_*` + Slack approvals vars (D-052 → CLEANUP-001), `AURINKO_*` (D-050 → Batch 4 retirement; still required-for-feature until then, marked both), `STRIPE_PRICE_*` legacy SKUs (→ P0-013).

## 2. The table

Read sites are `file:line` under `src/` (representative where a name has more than ~6 sites; the grep in §1 is exhaustive). "Production" = PRESENT / ABSENT / UNKNOWN per §1. "Disabled when absent" names the exact code path and the surface. "Owner-visible copy today" is what the owner actually sees — "silent" means nothing is shown.

| Var | Class | Read sites | Production | Disabled surfaces / paths when absent | Owner-visible copy today | Decision / ticket |
|---|---|---|---|---|---|---|
| `ANTHROPIC_API_KEY` | required | `lib/ai-service.ts:74`, `lib/agent-planner.ts:373`, `lib/bi-agent.ts:114`, `lib/draft-verifier.ts:121`, `lib/email-classifier.ts:78`, `lib/email-drafter.ts:90`, `lib/sms-classifier.ts:70`, `lib/sms-drafter.ts:80`, `lib/whisper.ts:107`, `lib/whisper-summary.ts:100`, `lib/recovery/extract.ts:128`, `lib/recovery/vehicle-llm.ts:48`; dynamic `lib/data/agents.ts:56` | UNKNOWN | Every model call throws `ANTHROPIC_API_KEY is not configured`: inbound SMS/email classification + drafting, Gradia Agent planner, Ask Gradia, Whisper, recovery extraction, draft verifier. Agent catalog prerequisites flip to not-done → agents show `needs_setup`/`off`. | Receptionist/agents catalog prerequisite "Anthropic key on server (…)" unchecked; failed runs surface as run errors. | Tier 1 in `env-setup.md`. E01-04 (ModelProvider seam) will move this behind the seam. |
| `AURINKO_API_BASE` | optional/dev-only (mock seam) | `lib/aurinko.ts:29` | UNKNOWN (must be absent in Production) | None — defaults to the real Aurinko API. | silent | Undocumented in both baselines (§3.1). Retires with D-050. |
| `AURINKO_CLIENT_ID` | required-for-feature · deprecated-by-decision (D-050, Batch 4) | `(dashboard)/settings/page.tsx:131`, `api/aurinko/auth/start/route.ts:51`, `lib/aurinko.ts:42` | UNKNOWN (precondition 4) | `/settings` **Email** and **Calendar** tiles → `available=false`; OAuth start returns HTTP 500 "Aurinko is not configured on this server yet."; token refresh throws → already-connected shops' email/calendar sync fails. | ~~Tile badge "Coming soon"~~ → **NOT AVAILABLE** (UX-001, 2026-09-02): "Email isn't set up for this workspace yet — we're finishing the connection on our side." No Connect control. | E02-03 (Google direct) / E02-06 (Aurinko retirement) own the tile copy; UX-001 (b) for the interim "Coming soon" misrepresentation. |
| `AURINKO_CLIENT_SECRET` | required-for-feature · deprecated-by-decision (D-050) | `(dashboard)/settings/page.tsx:132`, `lib/aurinko.ts:43` | UNKNOWN (precondition 4) | Same as `AURINKO_CLIENT_ID` (both are required together at `settings/page.tsx:130-133` and `aurinko.ts:42-46`). | Same. | Same. |
| `AURINKO_SIGNING_SECRET` | required-for-feature · deprecated-by-decision (D-050) | `lib/aurinko.ts:672` | UNKNOWN | `verifyAurinkoSignature` returns `false` → `/api/aurinko/webhook` answers 401 "Invalid signature" to every notification → **inbound email leads never arrive** while the Email tile still reads "Connected". (Since PR #34 `cdb0c99`, 2026-09-02, the subscription **validation ping** `?validationToken=` is echoed before signature verification, so a missing secret no longer blocks *connecting* — it only silences notifications.) | **silent** (fail-closed with no owner signal). | P0-012 should alert on sustained webhook 401s (§4). E02-03/E02-06. |
| `CRON_SECRET` | required (P0 invariant: all scheduled work fails closed) | `api/cron/agents/route.ts:23`, `automations:24`, `no-show-ladder:65`, `provider-events-prune:28` (P0-005A), `reconcile:26`, `recovery-retention:29`, `reminders:49`, `roi-receipt:55`, `voice-sync:29`, `api/admin/margin-report/route.ts:18`; dynamic `lib/data/agents.ts:57` | UNKNOWN (precondition 4) | All nine `vercel.json` crons + the margin report return 401 and log `[cron/<name>] CRON_SECRET not configured`: reminders, scheduled agents, automations (every 5 min), no-show ladder, daily reconciliation, recovery retention, provider_events pruning (P0-005A), ROI receipt, voice sync all stop. | Catalog prerequisite "Reminder cron secret on server" unchecked; otherwise **silent** — reminders simply do not go out. | Tier 1 in `env-setup.md`. P0-012 should alert on cron auth failures (§4). |
| `ENCRYPTION_KEY` | required | `lib/crypto.ts:43` | UNKNOWN | `encryptSecret` throws `CryptoConfigError` → every flow that stores a credential fails (Aurinko OAuth callback, Twilio BYO/subaccount tokens, Vapi per-shop server secret, Jobber/HCP tokens). `tryDecryptSecret` returns `null` (logs `[crypto] decryptSecret failed`) → every stored credential reads as missing: Twilio falls back to the **env master account** (`lib/twilio.ts:130`), Vapi falls back to the env-global secret, Aurinko sync fails. | **silent** — tiles still read "Connected" while nothing works. | SEV-0 in `exposed-credential.md`. §3.7: silent degradation into the shared Twilio master is a tenant-binding hazard → P0-012 alert + env validator follow-up. |
| `GLOBAL_DAILY_COST_CEILING_CENTS` | optional | `lib/monitoring.ts:129` | UNKNOWN | `global_ceiling` anomaly never fires (unset/0 disables by design). | silent (ops-only) | Not in `env-setup.md` (§3.1). P0-012 consumes anomalies. |
| `GRADIA_DASHBOARD_URL` | required-for-feature (absolute URLs) | 20 sites: `(dashboard)/settings/page.tsx:49`, `actions/a2p.ts:22`, `actions/billing.ts:23`, `actions/quotes.ts:39`, `actions/twilio-provision.ts:22`, `actions/voice-builder.ts:21`, `api/aurinko/auth/{start:32,callback:28}`, `api/jobber/auth/{start:24,callback:34}`, `api/housecallpro/auth/{start:25,callback:34}`, `api/stripe/connect/start/route.ts:27`, `api/twilio/{sms:85,sms/status:76,a2p/status:30}`, `api/cron/voice-sync/route.ts:38`, `lib/automation-sweeps.ts:62`, `lib/slack.ts:16`, `lib/twilio.ts:212` | **PRESENT** (founder-confirmed 2026-08-28) | If absent: Twilio status callbacks are not registered (`twilio.ts:212` → delivery status freezes at the create-response value); Stripe Checkout return URLs fall back to `http://localhost:3000` (`actions/billing.ts:23-29`); OAuth redirect bases, public quote links, A2P callback, voice-sync webhook URL, and the `/settings` webhook-URL display fall back to request-header detection where implemented. | n/a today (present). | Tier 1. §3.8 recommends P0-013 make the billing fallback fail closed. |
| `GRADIA_LLM_MODEL` | optional/dev-only (eval A/B override) | `lib/bi-agent.ts:27` | UNKNOWN (should be absent) | None — defaults to the locked model. | silent | Undocumented in both baselines (§3.1). E01-04 seam. |
| `HOUSECALLPRO_CLIENT_ID` | **REMOVED** — no reader since CLEANUP-001 (2026-09-01, D-052); founder deletes it from Production | `(dashboard)/settings/page.tsx:147`, `api/housecallpro/auth/start/route.ts:44`, `lib/housecallpro.ts:62` | UNKNOWN | `/settings` **Jobs — Housecall Pro** tile `available=false`; OAuth start 500 "Housecall Pro is not configured on this server yet."; token refresh throws. | **"Coming soon"** badge on a connector that is being deleted. | CLEANUP-001 removes the tile, routes, lib, env docs. Not in `env-setup.md` (§3.1). |
| `HOUSECALLPRO_CLIENT_SECRET` | **REMOVED** — no reader since CLEANUP-001 (2026-09-01, D-052); founder deletes it from Production | `(dashboard)/settings/page.tsx:148`, `lib/housecallpro.ts:68` | UNKNOWN | Same as above. | Same. | CLEANUP-001. |
| `JOBBER_CLIENT_ID` | required-for-feature | `(dashboard)/settings/page.tsx:143`, `api/jobber/auth/start/route.ts:43`, `lib/jobber.ts:49` | UNKNOWN | `/settings` **Jobs — Jobber** tile `available=false`; OAuth start 500 "Jobber is not configured on this server yet."; token refresh throws `JobberError(500)` → push of approved leads/bookings fails for connected shops. | ~~"Coming soon" badge~~ → **NOT AVAILABLE** (UX-001, 2026-09-02): "The Jobber connection isn't set up for this workspace yet — we're finishing it on our side." Card copy no longer names env vars. | Jobber stays optional (D-030; Q-20 open). E03-02 imports Jobber *export files* without this var. UX-001 (b) for copy. |
| `JOBBER_CLIENT_SECRET` | required-for-feature | `(dashboard)/settings/page.tsx:144`, `lib/jobber.ts:55` | UNKNOWN | Same as above. | Same. | Same. |
| `NEXT_PUBLIC_GRADIA_CONFLICT_ENFORCEMENT` | optional (rollout flag; only exact `"true"` enables; build-time inlined) | `lib/features.ts:72` | **ABSENT / not `"true"`** (recorded: enforcement OFF) | Conflict enforcement OFF — booking/reschedule/block-time behave pre-P0-004; advisory locking + `pending_action_id` idempotency stay active regardless (P0-004A). | silent | E02-05 (default-on). In `.env.example`, not in `env-setup.md` (§3.1). |
| `NEXT_PUBLIC_SENTRY_DSN` | optional | `instrumentation-client.ts:9` | UNKNOWN | Browser Sentry not initialised (SDK no-ops on empty DSN). | silent | P0-012 (alert delivery). |
| `NEXT_PUBLIC_SENTRY_ENVIRONMENT` | optional | `instrumentation-client.ts:21` | UNKNOWN | Env tag falls back to `NEXT_PUBLIC_VERCEL_ENV`, then `"development"`. | silent | — |
| `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` | required-for-feature (Stripe Connect — **dormant**: `FEATURES.integrations.payments=false`, `/api/stripe/connect` 404 via `proxy.ts:15`) | `components/gradia/stripe-embedded-onboarding.tsx:51` | UNKNOWN | Embedded Connect onboarding renders its error state on first render. Surface unreachable today. | Component error state (not reachable). | E05 / D-019 (Stripe Connect first). |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | required (boot) | `lib/env.ts:6`, `lib/supabase/middleware.ts:10`, `app/auth/callback/route.ts:7` | UNKNOWN (inferred present — no authenticated page can render without it) | `getSupabasePublicConfig()` throws `Missing NEXT_PUBLIC_SUPABASE_URL or NEXT_PUBLIC_SUPABASE_ANON_KEY` → every server/client Supabase client, the auth middleware, and the auth callback fail. | Hard failure (error boundary). | Tier 1. Only var pair covered by `src/lib/env.ts` today. |
| `NEXT_PUBLIC_SUPABASE_URL` | required (boot) | `lib/env.ts:5`, `lib/supabase/middleware.ts:9`, `lib/supabase/service.ts:8`, `app/auth/callback/route.ts:6` | UNKNOWN (inferred present) | As above, plus `createServiceClient()` throws → all webhooks, crons, approvals executor, MCP. | Hard failure. | Tier 1. |
| `NEXT_PUBLIC_VERCEL_ENV` | optional/dev-only (Vercel system var; exposed only if the project's "expose system env vars" setting is on) | `instrumentation-client.ts:22` | UNKNOWN | Browser Sentry env tag falls back to `"development"`. | silent | Auto-provided — never set by hand (`env-setup.md:25`). |
| `NEXT_RUNTIME` | optional/dev-only (auto-provided by Next.js) | `instrumentation.ts:30,33` | n/a (runtime-provided) | Sentry server init keyed on `nodejs`/`edge`. | silent | Auto-provided. |
| `NODE_ENV` | optional/dev-only (auto-provided) | `actions/shop.ts:47,142`, `api/aurinko/auth/start/route.ts:64,79`, `api/jobber/auth/start/route.ts:55`, `api/housecallpro/auth/start/route.ts:56`, `api/vapi/webhook/route.ts:172` | n/a (`production` on Vercel) | Governs cookie `secure` flags and the P0-007 production-guard fallback when `VERCEL_ENV` is unset. | silent | Auto-provided. |
| `OPS_ALERT_SMS_FROM` | required-for-feature (ops alerts, SMS channel — new in P0-012) | `lib/alerts.ts` (`smsConfig`) | ABSENT (new; founder sets) | SEV-0/1 SMS channel unconfigured; webhook unaffected. | silent (ops) | P0-012 / D-042 |
| `OPS_ALERT_SMS_TO` | required-for-feature (ops alerts, SMS channel — new in P0-012) | `lib/alerts.ts` (`smsConfig`) | ABSENT (new; founder sets) | As above (both required together). | silent (ops) | P0-012 / D-042 |
| `OPS_ALERT_WEBHOOK_URL` | required-for-feature (**founder ops alerts** — new in P0-012) | `lib/alerts.ts` (`webhookUrl`) | ABSENT (new; founder sets — P0-012 acceptance step 6) | Every alert (anomalies, tenant-scope violations, reconciliation drift, cron failures) stays console + Sentry only; `/api/health` reports `alerts.webhookConfigured: false`. | silent (ops) | P0-012 / D-042 |
| `OPENAI_API_KEY` | required | `lib/embeddings.ts:20`, `lib/whisper.ts:113`; dynamic `lib/data/agents.ts:151` | UNKNOWN | Embeddings throw `OPENAI_API_KEY is not configured` → `recordInteraction` logs `[memory] embed failed` and memory recall degrades to no vector search; Whisper transcription fails. | Memory agent prerequisite "OpenAI key on server (for embeddings)" unchecked; Whisper errors. | Tier 1. E01-04 seam. |
| `SENTRY_DSN` | optional | `instrumentation.ts:15,24` | UNKNOWN | Server Sentry no-op. | silent | P0-012. |
| `SENTRY_ENVIRONMENT` | optional | `instrumentation.ts:26` | UNKNOWN | Falls back to `VERCEL_ENV`, then `"development"`. | silent | — |
| `SLACK_BOT_TOKEN` | **REMOVED** — no reader since CLEANUP-001 (2026-09-01, D-052); founder deletes it from Production | `lib/slack.ts:173,326` | UNKNOWN (should be absent) | `chat.postMessage` path skipped (falls to webhook); `updateSlackForPending` returns early. Surface is structurally dormant anyway (`FEATURES.slackApprovals=false`; interactivity route refuses at `api/slack/interactivity/route.ts:43`). | silent | CLEANUP-001 deletes. |
| `SLACK_DEFAULT_CHANNEL_ID` | **REMOVED** — no reader since CLEANUP-001 (2026-09-01, D-052); founder deletes it from Production | `lib/slack.ts:174` | UNKNOWN | As above. | silent | CLEANUP-001. |
| `SLACK_SIGNING_SECRET` | **REMOVED** — no reader since CLEANUP-001 (2026-09-01, D-052); founder deletes it from Production | `lib/slack.ts:1195` | UNKNOWN | Interactivity signature verification returns `false` (route already gated off). | silent | CLEANUP-001. |
| `SLACK_WEBHOOK_URL` | **REMOVED** — no reader since CLEANUP-001 (2026-09-01, D-052); founder deletes it from Production | `lib/slack.ts:192` (the `lib/reconciliation.ts` read was **removed by P0-012** later this session — drift now goes through `lib/alerts.ts`) | UNKNOWN | Approval-card webhook fallback skipped (dormant). | silent | §3.9 resolved by P0-012; CLEANUP-001 may delete freely. |
| `STRIPE_API_BASE` | optional/dev-only (mock seam) | `lib/stripe.ts:19` | **ABSENT** (founder-confirmed 2026-08-28) | None — defaults to the real Stripe API. | silent | Documented in `.env.example:77-79` only (§3.1). |
| `STRIPE_CONNECT_CLIENT_ID` | required-for-feature (Stripe Connect — dormant, `integrations.payments=false`) | `(dashboard)/settings/page.tsx:140`, `actions/stripe-connect.ts:33`, `api/stripe/connect/start/route.ts:48`, `lib/stripe.ts:44` | UNKNOWN | `getStripeOnboardingSession` returns "Stripe isn't configured on the server yet."; Connect start 500. `stripeConfigured` feeds the payments section only when `integrationEnabled("payments")` (`settings/page.tsx:412-417`) — hidden today. | Not reachable today. | E05 / D-019. |
| `STRIPE_PRICE_CREDIT_PACK` | deprecated-by-decision (legacy SKU → P0-013) | `lib/stripe.ts:291` | **ABSENT by design** | Credit-pack checkout throws `StripeError(500, "STRIPE_PRICE_CREDIT_PACK is not configured")` before any Stripe call → billing action returns `{ ok: false, error }`. | Billing page action error text (legacy pricing copy — UX-001 (b) / P0-013). | P0-013 (D-034 tiers). |
| `STRIPE_PRICE_ID` | deprecated-by-decision (legacy $20 SKU → P0-013) | `lib/stripe.ts:269` | **ABSENT by design** | Subscription checkout fails closed before any Stripe call (proven at P0-010 acceptance). | Billing page action error. | P0-013. |
| `STRIPE_PRICE_MINUTE_PACK` | deprecated-by-decision (→ P0-013) | `lib/stripe.ts:292` | **ABSENT by design** | Minute-pack checkout fails closed. | Billing page action error. | P0-013. |
| `STRIPE_PRICE_VOICE_ADDON` | deprecated-by-decision (legacy +$29 SKU → P0-013) | `lib/stripe.ts:285` (also consumed by `api/stripe/webhook` via `voiceAddonPriceId()` for add-on detection) | **ABSENT by design** | Voice add-on checkout fails closed; webhook cannot recognise the add-on line item (moot until P0-013 maps tiers). | Billing page action error. | P0-013. |
| `STRIPE_SECRET_KEY` | required-for-feature (platform billing: checkout, portal, subscription lifecycle; also Connect) | `lib/stripe.ts:36`, `(dashboard)/settings/page.tsx:139`, `actions/stripe-connect.ts:32`, `api/stripe/connect/start/route.ts:47` | UNKNOWN | `platformKey()` throws `StripeError(500)` → every billing server action returns `{ ok: false }`; Connect paths 500. | Billing page action error. | P0-013. |
| `STRIPE_WEBHOOK_SECRET` | required-for-feature (billing lifecycle; P0 invariant — fail closed) | `lib/stripe.ts:447`; dynamic `lib/data/agents.ts:58` | UNKNOWN | `verifyStripeSignature` returns `false` → `/api/stripe/webhook` answers 401 to **every** event → plan/subscription/credit-grant state never updates from Stripe. | Catalog prerequisite "Paid-status webhook secret on server" unchecked; otherwise silent. | P0-013; P0-012 should alert on sustained webhook 401s. |
| `SUPABASE_SERVICE_ROLE_KEY` | required | `lib/supabase/service.ts:9` | UNKNOWN (inferred present — webhooks/crons would be dead otherwise) | `createServiceClient()` throws → all webhooks (Twilio, Vapi, Stripe, Aurinko), all crons, the approvals executor, MCP, and every service-role path (31-file inventory, P0-011). | Hard failures on those paths; dashboard reads still work. | Tier 1. |
| `TWILIO_ACCOUNT_SID` | required-for-feature (SMS tile; pilot master account; subaccount provisioning; A2P) | `(dashboard)/settings/page.tsx:135`, `lib/twilio.ts:69` (fallback resolution `:130`; sends/lookups `:269,341,410,442,475,531`) | UNKNOWN (precondition 4) | `/settings` **SMS** tile `available=false`. Shops without subaccount/BYO credentials → `resolveTwilioCredentials` → `null` → inbound SMS signature verification fails (401) and sends are refused as "Twilio not configured". Gradia-number provisioning (needs the master account) throws `TWILIO_ACCOUNT_SID or TWILIO_AUTH_TOKEN missing` (`twilio.ts:478`). | ~~"Coming soon" badge~~ → **NOT AVAILABLE** (UX-001, 2026-09-02): "Texting isn't set up for this workspace yet — we're finishing the connection on our side." | UX-001 (b) for copy; P0-008 credential model unchanged. |
| `TWILIO_AUTH_TOKEN` | required-for-feature | `(dashboard)/settings/page.tsx:136`, `lib/twilio.ts:65` | UNKNOWN (precondition 4) | Same as `TWILIO_ACCOUNT_SID` (pair). | Same. | Same. |
| `TWILIO_API_BASE` | optional/dev-only (mock seam) | `lib/twilio.ts:189`, `lib/twilio-a2p.ts:25` | UNKNOWN (must be absent) | None — real Twilio API. | silent | Undocumented in both baselines (§3.1). |
| `TWILIO_MESSAGING_API_BASE` | optional/dev-only (mock seam) | `lib/twilio-a2p.ts:23` | UNKNOWN (must be absent) | None. | silent | Undocumented (§3.1). |
| `TWILIO_PRIMARY_PROFILE_SID` | required-for-feature (A2P 10DLC, EIN path) | `lib/telephony-provider.ts:285` | UNKNOWN | Businesses with an EIN get `{ ok: false }` from carrier registration; sole-prop path unaffected. | "Carrier registration isn't configured on the server yet." | **Undocumented in both baselines** (§3.1). Capability 22 notes A2P SIDs unverified. |
| `TWILIO_TRUSTHUB_API_BASE` | optional/dev-only (mock seam) | `lib/twilio-a2p.ts:21` | UNKNOWN (must be absent) | None. | silent | Undocumented (§3.1). |
| `VAPI_API_BASE` | optional/dev-only (mock seam) | `lib/vapi.ts:17` | UNKNOWN (must be absent) | None. | silent | Undocumented (§3.1). |
| `VAPI_API_KEY` | required-for-feature (Voice) | `(dashboard)/settings/page.tsx:129`, `app/onboarding/page.tsx:63`, `lib/vapi.ts:29` | UNKNOWN (precondition 4) | `/settings` **Voice** tile `available=false`; onboarding voice-builder card disables its primary button (`voice-builder-card.tsx:268`); assistant create/update throws `VapiApiError(500)`. | ~~Tile "Coming soon"; onboarding "check back soon"~~ → **NOT AVAILABLE** (UX-001, 2026-09-02): "Voice isn't set up for this workspace yet — we're finishing the connection on our side." (tile + builder card). | UX-001 (b). |
| `VAPI_DEFAULT_SHOP_ID` | optional/dev-only (single-shop **dev** fallback; Production refuses it) | `api/vapi/webhook/route.ts:194` | **ABSENT** (founder-confirmed 2026-08-28; must stay absent) | None in Production by design — unmatched assistants fail closed (P0-007). If set in Production the code logs an error and still refuses (`isProductionRuntime()`), so it is inert, not dangerous. | silent | **§3.6: autorun precondition 4 lists it as founder-to-set — contradicts P0-007/P0-010.** |
| `VAPI_WEBHOOK_SECRET` | required-for-feature (assistants created before per-shop secrets) | `api/vapi/webhook/route.ts:163` | UNKNOWN | Legacy assistants without `vapi_server_secret_enc` → every webhook event 401 → calls not logged or metered. Assistants with a per-shop secret are unaffected. | **silent**. | Vapi vendor doc; P0-012 alert on webhook 401s. |
| `VERCEL_ENV` | optional/dev-only (Vercel system var) | `api/vapi/webhook/route.ts:170`, `instrumentation.ts:26` | UNKNOWN (platform-provided if system env exposure is on) | P0-007 production guard falls back to `NODE_ENV=production` (which is also true on Preview builds) → guard becomes *stricter* on Preview, never looser. Sentry env tag falls back. | silent | Auto-provided; §3.10. |

Row count: **53** = 50 at audit time + the three `OPS_ALERT_*` rows P0-012 added later the same session. Six rows (`HOUSECALLPRO_*` ×2, `SLACK_*` ×4) are now **REMOVED** by CLEANUP-001 — kept in the table as the record of what the founder should delete from Production; live reader count is 47.

## 3. Findings

### 3.1 Read in `src/` but undocumented
- **Missing from `.env.example`** (7 real + 4 auto-provided): `AURINKO_API_BASE`, `GRADIA_LLM_MODEL`, `TWILIO_API_BASE`, `TWILIO_MESSAGING_API_BASE`, `TWILIO_PRIMARY_PROFILE_SID`, `TWILIO_TRUSTHUB_API_BASE`, `VAPI_API_BASE` (+ `NEXT_PUBLIC_VERCEL_ENV`, `NEXT_RUNTIME`, `NODE_ENV`, `VERCEL_ENV`, which are correctly never set by hand). `TWILIO_PRIMARY_PROFILE_SID` is the only one that gates a **product surface** (A2P EIN path) — it should be documented first.
- **Missing from `docs/env-setup.md`** (15): the seven above plus `GLOBAL_DAILY_COST_CEILING_CENTS`, `HOUSECALLPRO_CLIENT_ID`, `HOUSECALLPRO_CLIENT_SECRET`, `NEXT_PUBLIC_GRADIA_CONFLICT_ENFORCEMENT`, `STRIPE_API_BASE`, `STRIPE_PRICE_CREDIT_PACK`, `STRIPE_PRICE_MINUTE_PACK`, `STRIPE_PRICE_VOICE_ADDON`. The P0-010 close already recorded the `*_API_BASE` / `GRADIA_LLM_MODEL` / `TWILIO_PRIMARY_PROFILE_SID` gap; this audit adds the rest.

### 3.2 Documented but never read in `src/`
- `META_APP_ID`, `META_APP_SECRET`, `META_WEBHOOK_VERIFY_TOKEN` — in both baselines; **no reader exists** (Meta routes removed under `GRADIA_IGFB_CHARGE_REMOVAL_PLAN.md`). Dead entries; remove at the next docs pass that touches `.env.example` (CLEANUP-001 is the natural host since it already edits env docs).
- `SENTRY_ORG`, `SENTRY_PROJECT`, `SENTRY_AUTH_TOKEN` — read at **build time** by `next.config.ts:14-17` (source-map upload), not by `src/`. Legitimate; note the "build" scope is already correct in `env-setup.md:148`.
- `SUPABASE_TEST_URL`, `SUPABASE_TEST_SERVICE_ROLE_KEY` — CI-integration only (`eval/integration/_db.ts`). Legitimate; never Production.
- `CREDIT_COST` in `env-setup.md:127` is a code constant (`src/lib/credits.ts`), not an env var — misleading in an env checklist.

### 3.3 Stale statements in `docs/env-setup.md` (dated 2026-06-01)
- Lines 21-23 banner: `STRIPE_PRICE_ID`, `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`, `VAPI_API_KEY` **are** in `.env.example` now; `META_APP_ID` has no reader. Banner is obsolete.
- Tier 4 heading "only when you want to charge $20/mo" and `STRIPE_PRICE_ID` "the $20/mo recurring Price id" — legacy price strings (D-031/D-034 supersede). "flip `FEATURES.paywall = true`" — `paywall` is already `true` (`features.ts`). Feature-flag section says `paywall: false` — wrong.
- Tier 2 "Slack is opt-in" — superseded by D-052 (delete).
- No Housecall Pro rows at all (the ticket already noted this) — moot once CLEANUP-001 lands.

### 3.4 The "Coming soon" misrepresentation list (owner-visible roadmap copy driven by server env absence)

> **Resolved 2026-09-02 by UX-001** (autorun Batch 1, item 3c): every row below now renders the honest **NOT AVAILABLE** state from one source (`STRINGS.connections.notAvailableReason`, `integrationAvailability()` in `src/lib/data/connections.ts`), with no Connect control and no env-var names; locked by `eval/ux-001-truthful-state.test.ts` (no "coming soon" in owner-visible source). The Billing row stays with P0-013. The table is kept as the audit record.
| Surface | Driven by | Copy today | Owner of the copy fix |
|---|---|---|---|
| `/settings` Voice tile | `VAPI_API_KEY` | "Coming soon" · "We'll let you know the moment it's ready." | UX-001 (b) |
| `/settings` Email tile | `AURINKO_CLIENT_ID` + `_SECRET` | same | E02-03 (calendar/email tiles); UX-001 (b) interim |
| `/settings` SMS tile | `TWILIO_ACCOUNT_SID` + `_AUTH_TOKEN` | same | UX-001 (b) |
| `/settings` Calendar tile | `AURINKO_CLIENT_ID` + `_SECRET` | same | E02-03; UX-001 (b) interim |
| `/settings` Jobs — Jobber tile | `JOBBER_CLIENT_ID` + `_SECRET` | same | UX-001 (b) |
| `/settings` Jobs — Housecall Pro tile | `HOUSECALLPRO_CLIENT_ID` + `_SECRET` | same | CLEANUP-001 (tile removed) |
| Onboarding voice-builder card | `VAPI_API_KEY` | "We're finishing voice setup on our side — check back soon." | UX-001 (b) |
| Billing page checkout actions | `STRIPE_PRICE_*` (absent by design) | server-action error text | P0-013 |

All six tiles share one renderer (`connection-tile.tsx:50-66`, comment "Built but not yet wired for this workspace"). The honest state is **NOT AVAILABLE / not configured on the server**, which is an operator condition, not a roadmap message. UX-001 (a)/(b) also owns the founder repro (Email tile says "Connect Gmail" while connected) — note that "Connect" showing at all means `AURINKO_CLIENT_ID/_SECRET` **were present** in whatever environment the repro ran on; if that was Production, those two rows are effectively PRESENT (not recorded as such here because the environment was not stated).

### 3.5 Dynamic reads / indirection (ticket failure case 2)
- `src/lib/data/agents.ts:39-40` — `envHas(name)` reads `process.env[name]` for `ANTHROPIC_API_KEY`, `CRON_SECRET`, `STRIPE_WEBHOOK_SECRET`, `OPENAI_API_KEY` (lines 56-58, 151). Presence only, never values. All four names are also read statically elsewhere, so the 50-name inventory is unchanged.
- `src/lib/env.ts` — `getSupabasePublicConfig()` is the only validator; consumed by `lib/supabase/server.ts` and `client.ts`. Covers exactly two public vars.
- No `process.env` destructuring, no `Object.keys(process.env)`, no config loader. Nothing else hides a read.

### 3.6 Contradiction: `VAPI_DEFAULT_SHOP_ID` in autorun precondition 4
`program/autorun.md` precondition 4 lists `VAPI_DEFAULT_SHOP_ID` among the Production vars the founder sets. P0-007 made the Production fallback **refuse** (`api/vapi/webhook/route.ts:194-204`), P0-010 scope item 9 had the founder **confirm it absent**, the handoff §32 lists it "ABSENT as required", and `.env.example:39-41` says "leave this blank in prod" (test-locked in `eval/production-surfaces.test.ts:61`). Setting it is inert (the code refuses it in Production) but contradicts the recorded posture and would make the P0-007 acceptance evidence stale. **Recommendation: strike it from precondition 4; six vars, not seven.** Needs founder confirmation — recorded in `autorun-log.md`.

### 3.7 `ENCRYPTION_KEY` absence degrades silently into the shared Twilio master account
With the key missing, `tryDecryptSecret` returns `null` for every stored credential and `resolveTwilioCredentials` falls through to the env master account (`lib/twilio.ts:130`), so a subaccount/BYO shop's inbound verification and sends would silently run against Gradia's master credentials instead of failing. Nothing tells the owner or the operator. This is the strongest argument for (a) a `TENANT_SCOPE_VIOLATION`-style structured alert on decrypt failure (P0-008 residual M1, P0-012 consumer) and (b) a boot-time validator (§4, ENV-VALIDATOR follow-up).

### 3.8 `GRADIA_DASHBOARD_URL` billing fallback is fail-open
`actions/billing.ts:23-29` falls back to `http://localhost:3000` for Stripe Checkout success/cancel URLs when the var is absent or malformed. Present today, so no live impact — but P0-013 should make that path fail closed (return `{ ok: false }`) rather than hand Stripe a localhost redirect.

### 3.9 `SLACK_WEBHOOK_URL` has two readers with different fates — **RESOLVED later this session by P0-012** (reconciliation now emits through `lib/alerts.ts`; the only remaining reader is `lib/slack.ts`, which CLEANUP-001 deletes)
`lib/slack.ts:192` (approvals — deleted by D-052/CLEANUP-001) and `lib/reconciliation.ts:86` (daily ledger-vs-Twilio drift alert — an **ops** alert that D-042 says belongs on the P0-012 seam, independent of `lib/slack.ts`). CLEANUP-001 must either leave the reconciliation read in place until P0-012 re-homes it, or P0-012 (queue item 3, which runs first) should absorb it. Sequencing note for both tickets.

### 3.10 `VERCEL_ENV` exposure is an assumption
The P0-007 production guard prefers `VERCEL_ENV`; if the Vercel project does not expose system env vars, the guard falls back to `NODE_ENV`, which is `production` on Preview too. Direction of failure is stricter (Preview also refuses the dev fallback), so no risk — but the assumption should be confirmed once when the founder runs `vercel env ls production` (§4).

### 3.11 No central validator
`src/lib/env.ts` validates two vars. The other 48 fail at first use, each with a different error shape, and several fail **silently** from the owner's point of view (`AURINKO_SIGNING_SECRET`, `VAPI_WEBHOOK_SECRET`, `CRON_SECRET`, `ENCRYPTION_KEY`, `STRIPE_WEBHOOK_SECRET`). Recommend a follow-up ticket (working name **ENV-VALIDATOR**): a boot-time presence check for the *required* class (fail loud), a Production-only refusal list (`VAPI_DEFAULT_SHOP_ID`, `*_API_BASE`, `GRADIA_LLM_MODEL`), and a source-scan test that locks this 50-name inventory the way P0-011 locked the service-role importer inventory. Not done here (explicit non-goal).

## 4. Recommended founder actions and owning tickets

**Founder (Lane A, manual, read-only unless stated):**
1. Run `vercel env ls production` once (names only) and mark the UNKNOWN rows in §2 PRESENT/ABSENT — or install the CLI so the next autorun session can. Redact anything that is not a name before pasting.
2. Confirm precondition 4 with **six** vars: `VAPI_API_KEY`, `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `AURINKO_CLIENT_ID`, `AURINKO_CLIENT_SECRET`, `CRON_SECRET`. Leave `VAPI_DEFAULT_SHOP_ID` absent (§3.6).
3. Confirm the three silent-failure secrets are present: `ENCRYPTION_KEY`, `STRIPE_WEBHOOK_SECRET`, `AURINKO_SIGNING_SECRET` (and `VAPI_WEBHOOK_SECRET` if any assistant predates per-shop secrets).
4. Confirm the Vercel project exposes system env vars (`VERCEL_ENV`) — §3.10.
5. Keep `STRIPE_PRICE_*` absent until P0-013 is implemented, reviewed, accepted (unchanged posture).

**Owning tickets (recommendations only — no code in this ticket):**
| Item | Ticket |
|---|---|
| "Coming soon" → honest not-configured copy on Voice/SMS/Jobber tiles + onboarding voice card | ~~UX-001 (b)~~ **done 2026-09-02 (UX-001, all tiles incl. Email/Calendar interim)** |
| Email/Calendar tile copy + Aurinko retirement | E02-03 / E02-06 (D-050) |
| Housecall Pro tile, routes, lib, env docs, `META_*` dead entries, Slack approvals vars | CLEANUP-001 (D-052) |
| Legacy price strings in `env-setup.md` Tier 4 and billing action errors; `STRIPE_PRICE_*` re-shape; billing URL fail-closed (§3.8) | P0-013 (D-034/D-035) |
| Alerts on cron 401 / webhook 401 / decrypt failure; reconciliation drift alert re-home (§3.9); Sentry DSN presence | P0-012 (D-042) |
| `env-setup.md` stale banner + flag section + `CREDIT_COST` line; document `TWILIO_PRIMARY_PROFILE_SID`, `GLOBAL_DAILY_COST_CEILING_CENTS`, `NEXT_PUBLIC_GRADIA_CONFLICT_ENFORCEMENT`, seams | next docs pass that touches env docs (CLEANUP-001 or the P0-013 closeout) |
| Boot-time validator + inventory lock test | **ENV-VALIDATOR** (new, to be cut by the Organizer; §3.11) |
| `ANTHROPIC_API_KEY` / `OPENAI_API_KEY` / `GRADIA_LLM_MODEL` behind the provider seam | E01-04 |

## 5. Acceptance record (Builder, 2026-09-01)

1. Grep re-run: 50 deduped names; every §2 row carries ≥ 1 `file:line`. ✓
2. Disabled states verified locally with the vars absent, via a throwaway vitest file run inside `eval/` (not committed, deleted after; 6/6 passed): `ConnectionTile available=false` renders "Coming soon" + the wait line and no Connect control (covers the six tile rows); `resolveTwilioCredentials(null)` → `null` and `defaultStatusCallbackUrl()` → `null` with `TWILIO_*`/`GRADIA_DASHBOARD_URL` absent; `verifyStripeSignature`/`verifyAurinkoSignature` → `false` with secrets absent; `getSupabasePublicConfig()` throws with the public Supabase vars absent; `FEATURES.conflictEnforcement` → `false` with the flag absent; `encryptSecret` throws `CryptoConfigError` and `tryDecryptSecret` → `null` with `ENCRYPTION_KEY` absent. ✓
3. Secret-shape scan of this file (`sk_`, `whsec_`, `AC[0-9a-f]{32}`, `eyJ`, credentialed URLs): see the commit's validation line in `program/autorun-log.md`. ✓
4. Reviewer spot-check of five rows against code: **assigned to the Cursor Reviewer** (batch review, `autorun-log.md`).

## 6. Rollback
Delete this file and revert the five one-line cross-links/amendments listed in the ticket. No other state.
