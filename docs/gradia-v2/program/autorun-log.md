# AUTORUN log

_Append-only. One block per ticket. Builder writes plan + result; Reviewer appends verdict; founder appends acceptance/merge. Format below._

```
## <TICKET> — <title>
- Session: <date/time> · Batch: <n> · Branch: <name>
- Plan: (≤10 lines)
- Result: DONE | BLOCKED | STOPPED — commit <hash>
- Validation: unit <n> · int <n> · tsc ✓ · lint ✓ · build ✓
- Residuals: MEDIUM … / LOW …
- Needs founder: …
- Reviewer: (verdict, findings, review-fix commit)
- Founder: (acceptance PASS/FAIL, merged <hash>)
- NEXT: <next queue item>
```

## PROD-CONFIG-AUDIT — Production configuration audit (docs-only)
- Session: 2026-09-01 21:17 PDT · Batch: 1 · Branch: auto/batch-1 (from main `953cdd5`)
- Preconditions checked at session start: PR #29 MERGED (squash `e02c81a`); main carries D-034/D-035 (`11-decision-log.md:87-88`) and D-053; `auto/batch-1` = `origin/main`; stashes unchanged (3); worktree noise only `.playwright-mcp/` + Cursor's uncommitted SITE-READ markers in `SITE_SYNC.md`. Queue item 1 is first — no prior item to close.
- Plan: (1) grep every `process.env` read under `src/` (both forms + dynamic `envHas` + `env.ts` indirection) → 50 names, matches the Organizer sweep; (2) read each site's absent-var behaviour; (3) presence via `vercel env ls production` only if authenticated — CLI not installed, no login attempted → UNKNOWN except recorded facts (P0-010 close, D-034 guard, P0-004 flag); (4) compare against `docs/env-setup.md` + `.env.example` (computed set differences, not eyeballed); (5) write the runbook: method, 50-row table, findings, founder actions + owner tickets; (6) one-line cross-links ×3; (7) D-036/D-048/D-049 ICP amendments ×2; (8) acceptance: grep re-run, disabled states executed with vars absent, secret-shape scan; (9) full gate; (10) one commit, exact files.
- Result: DONE — commit `0043401`
- Files: `docs/gradia-v2/runbooks/production-config-audit.md` (new) · `docs/env-setup.md` · `docs/gradia-v2/runbooks/exposed-credential.md` · `docs/gradia-v2/program/capability-status.md` · `docs/gradia-v2/ui/design-north-star.md` · `docs/gradia-v2/ui/navigation-model.md`. No `src/` changes.
- Validation: unit 660 passed / 4 skipped (65 files) · int 102 passed / 7 skipped (11 files, local Supabase) · tsc ✓ · lint ✓ · build ✓ · doc secret-shape scan 0 hits · table rows 50 = grep inventory (diff identical)
- Manual acceptance: step 1 executed (50/50 rows with file:line); step 2 executed — throwaway vitest file inside `eval/` (never staged, deleted), 6/6 passed: ConnectionTile `available=false` → "Coming soon" + wait line, no Connect; Twilio creds/status-callback → null; Stripe/Aurinko signature verify → false; Supabase public config → throws; conflict flag → OFF; ENCRYPTION_KEY → encrypt throws / decrypt null; step 3 executed (0 hits); step 4 assigned to the Cursor Reviewer (five-row spot-check).
- Residuals: MEDIUM — presence column is UNKNOWN for 40 of 50 names until the founder runs `vercel env ls production` (§4.1); `SLACK_WEBHOOK_URL` reconciliation read must be sequenced between P0-012 and CLEANUP-001 (§3.9); `ENCRYPTION_KEY` absence degrades silently into the shared Twilio master (§3.7 — P0-012 alert + ENV-VALIDATOR). LOW — `env-setup.md` stale banner/flag section/`CREDIT_COST` line (§3.3, next env-docs pass); `GRADIA_DASHBOARD_URL` billing fallback fail-open (§3.8 → P0-013); `VERCEL_ENV` exposure assumption (§3.10, stricter direction).
- Needs founder: (a) **precondition 4 lists `VAPI_DEFAULT_SHOP_ID` as a Production var to set — contradicts P0-007 (fallback refused in prod), P0-010 (confirmed ABSENT) and `.env.example`; recommend six vars, not seven** (§3.6); (b) run `vercel env ls production` once (names only) and fill the UNKNOWN rows, or install the CLI for autorun; (c) confirm `ENCRYPTION_KEY`, `STRIPE_WEBHOOK_SECRET`, `AURINKO_SIGNING_SECRET`, `VAPI_WEBHOOK_SECRET` present (silent-failure class); (d) confirm Vercel system env exposure (`VERCEL_ENV`); (e) Organizer to cut **ENV-VALIDATOR** (§3.11) if agreed.
- Process notes: `program/capability-status.md` received its one-line cross-link because ticket scope 5 assigns it explicitly (autorun rule 6 vs ticket scope — resolved per autorun.md "ticket wins on scope"). `SITE_SYNC.md` carries Cursor's uncommitted SITE-READ markers, so the Builder appends its block but never stages that file. This log block is written after the commit per rule 4 and will be staged with the next ticket's commit.
- Reviewer: **PASS** (Cursor, 2026-09-02). Five-row spot-check against commit `0043401` (ticket acceptance step 4): `ENCRYPTION_KEY` `lib/crypto.ts:43` throws `CryptoConfigError` when absent; `VAPI_DEFAULT_SHOP_ID` `api/vapi/webhook/route.ts:194` is refused in production; `STRIPE_WEBHOOK_SECRET` `lib/stripe.ts:447` returns false when absent; `TWILIO_ACCOUNT_SID` `settings/page.tsx:135` + `lib/twilio.ts:69`; `AURINKO_SIGNING_SECRET` `lib/aurinko.ts:672` returns false when absent. Table row count and classes match the grep inventory at that commit. Secret-shape scan: the only `sk_` / `whsec_` / `eyJ` hits are the regex names in §5, not values. No BLOCKER/HIGH. No review-fix commit. LOW (later-ticket drift, not this commit): `GRADIA_DASHBOARD_URL` read-sites still list `lib/slack.ts` and `/api/housecallpro/auth/*` after CLEANUP-001 deleted those files.
- Founder: merged to `main` 2026-09-02 in PR #33 (squash `ff66cc9`; `ci / checks` + `ci-integration / integration` + Vercel green). Not acceptance-gated. **Organizer close 2026-09-02: DONE.** Needs-founder (a) resolved in this closeout (autorun precondition 4 corrected to six vars — `VAPI_DEFAULT_SHOP_ID` removed); (b)–(e) carried to `backlog.md` → Band 2 "Batch 1 residuals" (presence fill via `vercel env ls production`, silent-failure secret check, `VERCEL_ENV` exposure, ENV-VALIDATOR not cut yet).
- NEXT: P0-005A provider_events retention/pruning (queue item 2)

## P0-005A — provider_events retention and pruning
- Session: 2026-09-01 21:27 PDT · Batch: 1 · Branch: auto/batch-1
- Previous item: PROD-CONFIG-AUDIT DONE at `0043401` (not acceptance-gated) → proceed.
- Plan: (1) read ticket + ADR-001 C2 + migration 20260812120000 + claim helper + existing retention cron; (2) policy: completed 30d / failed 90d / floor 7d clamped in SQL / processing never — floor = 2× Stripe's ~3-day retry horizon (longest in the stack); (3) one additive migration: partial index on completed rows + service-role-only `prune_provider_events` RPC, bounded oldest-first `LIMIT … FOR UPDATE SKIP LOCKED` deletes per terminal status, jsonb report with hardening counts; (4) `provider-events-retention.ts` constants + bounded loop (claim module untouched); (5) daily cron route + vercel.json; (6) extend the P0-011 importer inventory (tenant-blind route); (7) unit + real-Postgres integration suites incl. concurrency and the replay tradeoff; (8) apply locally via `supabase migration up --local`, re-apply via container psql, EXPLAIN with 30k rolled-back rows; (9) ticket + ADR-001 + audit-doc records; (10) gate, one commit.
- Result: DONE — commit `0b4426d`
- Files: `supabase/migrations/20260901120000_provider_events_pruning.sql` · `src/lib/provider-events-retention.ts` · `src/app/api/cron/provider-events-prune/route.ts` · `vercel.json` · `eval/tenant-scoping.test.ts` (REVIEWED_IMPORTERS +1) · `eval/provider-events-prune.test.ts` (new, 15) · `eval/integration/provider-events-prune.int.test.ts` (new, 6) · `docs/gradia-v2/tickets/P0-005A-…md` (record appended; Status line left to the Organizer) · `docs/gradia-v2/adr/ADR-001-…md` (addendum) · `docs/gradia-v2/runbooks/production-config-audit.md` (CRON_SECRET row: nine crons) · `program/autorun-log.md` (previous block).
- Validation: unit 675 passed / 4 skipped (66 files; was 660) · int 115 passed / 0 skipped (12 files; was 102 + 7 skipped — session tests now run because the local ANON key was supplied) · tsc ✓ · lint ✓ · build ✓ (`ƒ /api/cron/provider-events-prune`) · migration applied locally + re-applied idempotently (psql in the local container) · EXPLAIN: `Index Scan using provider_events_completed_prune_idx` → LockRows → Limit on 30k synthetic rows (rolled back) · one call pruned exactly 5,000.
- DB-sensitive slot: this ticket shipped **one additive migration** (the ticket allowed zero or one) — occupies the slot during review; the Organizer records it at close. No RLS change (table stays deny-all; RPC service-role only).
- Manual acceptance: step 1 executed locally (integration suite + rolled-back 30k-row run); step 2 registration executed (unit-locked); **first scheduled run + log line on staging/Preview assigned to the founder** after the batch deploys — hold out of done until confirmed.
- Residuals: LOW — metadata/attempt CHECK constraints split (surfaced as WARN counts only); LOW — hardening counts are one sequential scan of the bounded table per day; LOW — RPC accepts any window ≥ 7d from any service-role caller (cron passes constants).
- Needs founder: none new. Organizer: ratify the 30/90/7 windows at closeout (ticket delegated the window to in-ticket approval); the tickets index/boards still show P0-005A as ready.
- Reviewer: **PASS** (Cursor, 2026-09-02). Falsifications run against local Postgres (`supabase_db_gradia-app`): (1) two concurrent `FOR UPDATE SKIP LOCKED` deletes on a 40-row expired backlog returned disjoint UUID sets (20/20, zero overlap, leftover empty); (2) `p_completed_retention_days = 0` / `p_failed_retention_days = 0` reports `completed_retention_days=7` and a 5-day completed row survives; (3) both anon and an authenticated owner session `rpc("prune_provider_events")` are denied. Existing unit (15) + int (6) suites re-run green. `provider-events.ts` claim paths untouched; RPC is service-role-only; `processing` never in a DELETE. No BLOCKER/HIGH. No review-fix commit. Builder residuals stand (CHECK constraints split; per-run hardening scan; RPC callable at any window ≥ 7d by service-role).
- Founder: merged to `main` 2026-09-02 in PR #33 (`ff66cc9`). Not acceptance-gated. **Organizer close 2026-09-02: DONE** — migration `20260901120000_provider_events_pruning.sql` shipped; DB-sensitive slot released at close. Carried to `backlog.md` (Batch 1 residuals): the first scheduled prune run + its log line (founder reads `vercel logs`; `/api/health` shows the `provider-events-prune` heartbeat) and founder ratification of the **30 / 90 / 7** windows — Builder-proposed, in force as shipped until ratified or changed.
- NEXT: P0-012 monitoring alert delivery / incident hooks (queue item 3)

## P0-012 — Monitoring alert delivery and incident hooks
- Session: 2026-09-01 21:39 PDT · Batch: 1 · Branch: auto/batch-1
- Previous item: P0-005A DONE at `0b4426d` (not acceptance-gated) → proceed. Decision present: D-042 (Q-08 resolved).
- Plan: (1) read ticket, D-042, severity runbook, monitoring/reconciliation, all cron routes, Sentry wiring; (2) `lib/alerts.ts` seam: env webhook + SEV-0/1 SMS, fail-open, dedupe, validation, Sentry hook, self-status, no `lib/slack.ts` dependency; (3) wire anomalies / tenant-scope violation / reconciliation drift; (4) `lib/cron-run.ts` wrapper + `cron_heartbeats` migration (no reusable stamp existed) applied to all nine cron routes; (5) `GET /api/health` with disclosure review; (6) `POST /api/admin/alert-test` for step 6; (7) convention section + runbook/vendor/env docs; (8) unit + integration suites incl. two-way registration lock; (9) gate, one commit.
- Result: DONE (steps 1–5) — commit `12fe1b0`. Step 6 (real destination + one real alert received) is founder-only and holds the ticket out of done.
- Files: `src/lib/alerts.ts` · `src/lib/cron-run.ts` · `src/app/api/health/route.ts` · `src/app/api/admin/alert-test/route.ts` · `src/lib/monitoring.ts` · `src/lib/reconciliation.ts` · 9 × `src/app/api/cron/*/route.ts` (GET → runCron) · `supabase/migrations/20260901130000_cron_heartbeats.sql` · tests `eval/alerts.test.ts` `eval/cron-run.test.ts` `eval/health.test.ts` `eval/integration/cron-heartbeats.int.test.ts` `eval/tenant-scoping.test.ts` · `.env.example` `docs/env-setup.md` `08-security-and-reliability.md` `runbooks/incident-severity.md` `vendors/core/sentry.md` `runbooks/production-config-audit.md` `tickets/P0-012-…md` · `program/autorun-log.md` (previous block).
- Validation: unit 703 passed / 4 skipped (69 files; was 675) · int 117 passed (13 files; was 115) · tsc ✓ · lint ✓ · build ✓ (`ƒ /api/health`, `ƒ /api/admin/alert-test`) · migration applied locally + re-applied idempotently.
- DB-sensitive slot: **one additive migration** (`cron_heartbeats`; ticket allowed at most one) — second DB-sensitive ticket on this batch branch after P0-005A; sequential, never concurrent; Organizer records at close.
- Severity mapping: TENANT_SCOPE_VIOLATION SEV-0 · global_ceiling SEV-1 · reconciliation drift SEV-1 · spend_spike/margin_floor SEV-2 · cron failure SEV-2 · test alert SEV-3 (table in the ticket record).
- Manual acceptance: steps 2–5 locked by tests locally (anomaly → alert payload; cron failure → one alert + failure stamp, next success clears; health shape/status codes/disclosure snapshot; unconfigured path identical + reported); **steps 1–6 live on staging/Production assigned to the founder**; step 6 closes the ticket.
- Residuals: MEDIUM — dedupe per instance (cross-instance window needs a table or the P10 queue); LOW — no rate limit on `/api/health`; LOW — per-item handled failures inside a successful sweep don't alert individually (logs only; P10 metrics); LOW — Sentry rules are recommendations.
- Needs founder: (a) set `OPS_ALERT_WEBHOOK_URL` (+ optional `OPS_ALERT_SMS_TO/FROM`) in Production after the batch deploys, then `POST /api/admin/alert-test` with the CRON_SECRET bearer and confirm receipt in writing (ticket DoD); (b) point an uptime pinger at `GET /api/health`; (c) click through the five recommended Sentry rules in `vendors/core/sentry.md`. CLEANUP-001 note: the reconciliation `SLACK_WEBHOOK_URL` read is already gone — `lib/slack.ts` can be deleted without an alerts regression.
- Reviewer: **PASS** (Cursor, 2026-09-02). Falsifications: (1) `runCron` handler 500 + destination HTTP 500 → cron returns its own `{ ok: false, error: "sweep failed" }`, no throw; (2) 100× same `(severity, source, title)` → 1 delivered + 99 suppressed, one more after the 10 min window; (3) `GET /api/health` with DB error `FATAL: password authentication failed… OPS_ALERT_WEBHOOK_URL` → 503 `down`, body contains none of FATAL/password/postgres/OPS_ALERT; (4) wrong bearer on all nine `CRON_REGISTRY` routes → 401 and zero `cron_heartbeats` upserts. Unit suites re-run green (`alerts` 14, `cron-run` 12, `health` 4). No BLOCKER/HIGH. No review-fix commit. MEDIUM residual confirmed: dedupe is per-instance (documented). LOW: `cron_heartbeats` relies on RLS-with-no-policies rather than the `REVOKE`/`GRANT` pair used on `provider_events` — anon SELECT returns `[]` (int-locked), not a leak; missing `CRON_SECRET` stays a silent 401 by ticket design.
- Founder: merged to `main` 2026-09-02 in PR #33 (`ff66cc9`). Not acceptance-gated. **Organizer close 2026-09-02: DONE (steps 1–5)** — migration `20260901130000_cron_heartbeats.sql` shipped; DB-sensitive slot released at close. **Step 6 is a founder action carried to `backlog.md`:** set `OPS_ALERT_WEBHOOK_URL` (+ optional `OPS_ALERT_SMS_TO/FROM`) in Production, `POST /api/admin/alert-test` with the `CRON_SECRET` bearer, confirm receipt in writing; also the uptime pinger on `GET /api/health` and the five Sentry rules. Until then every alert is console + Sentry only (fail-open, reported on `/api/health`).
- NEXT: CLEANUP-001 remove Housecall Pro connector + Slack approvals surface (queue item 3b)

## CLEANUP-001 — Remove Housecall Pro connector + Slack approvals surface (D-052)
- Session: 2026-09-01 21:53 PDT · Batch: 1 · Branch: auto/batch-1
- Previous item: P0-012 DONE (steps 1–5) at `12fe1b0`; not acceptance-gated → proceed. Decisions present: D-052, D-042.
- Plan: (1) sweep + dispositions (P3-001's scope, executed here); (2) delete 9 files, move the vendor doc to `removed/`; (3) brace-matched removal of every `send*ApprovalRequest` try/catch (29 sites) — staging untouched, executor untouched; (4) payment notices → `sendOpsAlert` SEV-3; (5) HCP out of seam/settings/customer page/shop actions, columns marked dormant, drop file in `supabase/rollbacks/` (not applied); (6) copy + one prompt clause, comments; (7) tests amended by removal only + new source-scan suite; (8) env/vendor/capability/roadmap/ADR/config-audit docs; (9) gate + live eval; (10) one commit.
- Result: DONE — commit `4bd2468`
- Validation: unit **692 passed / 4 skipped (68 files)** — accounting: 703 − 18 removed (slack-interactivity 5, crm-housecallpro 9, webhooks Slack-signature block 4) + 7 new = 692 ✓ · int 117 passed (13 files, unchanged) · tsc ✓ · lint ✓ (0 warnings after removing four dead `crossChannelHint` reads + two unused type imports) · build ✓ · Tier-1 `honest-errors` ✓ · **live `owner-agent-routing.eval.test.ts`: 5/6 — the failing case ("stages a reply for approval…", model never calls `draft_reply`) fails identically on the pre-CLEANUP commit `12fe1b0` (verified in a detached worktree) → pre-existing, not a regression.**
- Migration: none (rollback file only). DB-sensitive slot not occupied.
- Manual acceptance: steps 1–4 executed via tests + suites (close record); step 5 assigned to the Cursor Reviewer (accounting table).
- Residuals: LOW — `pending_actions.slack_*`/`decided_by_slack` + `slackUserId` decider stay (historical); LOW — HCP dormant columns until the founder runs the rollback file; LOW — historical docs still narrate Slack/HCP (Organizer wording pass); LOW — pre-existing owner-agent live-eval failure needs its own triage.
- Needs founder: (a) remove `HOUSECALLPRO_CLIENT_ID`, `HOUSECALLPRO_CLIENT_SECRET`, `SLACK_BOT_TOKEN`, `SLACK_SIGNING_SECRET`, `SLACK_DEFAULT_CHANNEL_ID`, `SLACK_WEBHOOK_URL` from Vercel Production (no reader remains; ticket DoD); (b) decide when to run `supabase/rollbacks/cleanup-001_hcp_slack_columns_drop.sql` (optional, destructive, by hand); (c) Organizer: flip P3-001 index row to superseded, backlog row 3b, record TS-6 closure in the P0-011 follow-ups; triage the owner-agent live-eval failure (backlog).
- Reviewer: **PASS** with one MEDIUM residual (Cursor, 2026-09-02). Falsifications: (1) `pending_actions` inserts are byte-identical on a dashboard importer (`actions/leads.ts`) and an inbound importer (`api/twilio/sms/route.ts`) — Slack `try/catch` sat after the insert; staging paths untouched. (2) `src/lib/approvals.ts` diff is the `sendSmsApprovalRequest` import, one post-insert `try/catch` in `queueBookingConfirmationSms`, plus comments — no executor / claim / finalize change (autorun rule 5 boundary held). (3) Accounting: deleted files carry 5 + 9 tests; `webhooks.test.ts` lost the 4-test Slack signature block; +7 source-scan; 703 − 18 + 7 = 692 at `4bd2468`. Surviving Twilio/Vapi/Aurinko tests still assert staging and replay. `eval/cleanup-001-removal.test.ts` green. No BLOCKER/HIGH. No review-fix commit. **MEDIUM:** ticket required “Stripe `invoice.paid` / `payment_failed` / `charge.refunded` → one SEV-3 alert each, none on retry” and claimed `provider_events` + burst dedupe cover it. The Stripe webhook never claims `provider_events`; dedupe is 10 min / per-instance. A Stripe retry after the window (or on a new lambda) re-notifies. Ledger writes stay unique-keyed — ops noise only, not a money-path bug. LOW: `ae7423f` was a docs follow-up (vendor banner + this log) left out of `4bd2468`.
- Founder: merged to `main` 2026-09-02 in PR #33 (`ff66cc9`). Not acceptance-gated. **Organizer close 2026-09-02: DONE.** Carried to `backlog.md` (Batch 1 residuals): the Reviewer's MEDIUM — Stripe `invoice.paid` / `payment_failed` / `charge.refunded` SEV-3 notices dedupe only 10 min per instance because the Stripe webhook never claims `provider_events`, so a late retry re-notifies (ops noise; ledger writes stay unique-keyed); removal of the six dead `HOUSECALLPRO_*` / `SLACK_*` Production vars (founder); the optional column-drop rollback file (destructive, founder by hand, no date); the pre-existing owner-agent live-eval failure (triage). Organizer items already satisfied on `main`: P3-001 index row superseded, backlog row 3b, TS-6 closure in the P0-011 follow-ups.
- Process note: this block was appended before the commit landed (a shell word-splitting slip stalled `git add`); hash corrected to `4bd2468`; `autorun-log.md` itself is not in `4bd2468` and rides the next commit.
- NEXT: UX-001 truthful state + polish pass (queue item 3c — founder visual review on Preview; check `ui/reference-board.md` precondition first)

## UX-001 — Truthful state + polish pass
- Session: 2026-09-02 · Batch: 1 · Branch: auto/batch-1
- Previous item: CLEANUP-001 DONE at `4bd2468` (+ docs follow-up `ae7423f`); not acceptance-gated → proceed. Reference-board Stripe adopt/reject lists present (`ui/reference-board.md` §Stripe Dashboard).
- Plan: (Builder omitted this log block; reconstructed from `feat(UX-001)` `038851c` and the ticket close record.) One `connectionStatus()` predicate set; NOT AVAILABLE replaces Coming soon; HelpTip composition; loading.tsx for five real routes; Home/Settings parity tests.
- Result: DONE (steps 1–3) — commit `038851c`. Step 4 (founder visual review on Preview) holds the ticket out of done.
- Validation: unit 729 passed / 4 skipped (claimed at commit; reviewer re-ran connection-truth + ux-001-truthful-state + onboarding suites green) · int 117 · tsc · lint · build green (Builder). Reviewer did not re-run the full gate.
- Residuals: MEDIUM — `approvals.ts` executor toasts still say “Connect Gmail via Aurinko” (copy-only, HARD-STOP class for edits). LOW — HelpTip is a new file despite “no new components” (composition of the Tooltip primitive; documented). LOW — ticket asked for tooltip focus/escape + reduced-motion tests; the trigger is a real `<button aria-label>` but those tests were not added. LOW — empty-state copy still inline on some routes; `agent-runtime.ts` still keys email off the token alone.
- Needs founder: visual review on Preview (manual acceptance step 4) — Email tile + Home card + Email card all read Connected on the founder’s shop; compare routes against the reference-board adopt list; PASS/FAIL here.
- Reviewer: **PASS** (Cursor, 2026-09-02). Root-cause matches the code: Settings/Email/onboarding keyed `connected` off `aurinko_account_email`; Home keyed off the credential pair; `connectionStatus()` now owns both and the founder-repro fixture (token + account_id, email null) renders Connected on the tile and Live on Home (`eval/connection-truth.test.ts`). Settings tiles bind `connected={connection.*}` / `available={availability.*}`; source-scan locks no “Coming soon”, no Aurinko/Slack in owner literals, every real dashboard page has a Skeleton `loading.tsx`. Shop switcher ruled out as the split (pure per-row helper). No BLOCKER/HIGH. No review-fix commit. Process: Builder did not append this log block or a SITE_SYNC entry (autorun rule 4) — recorded here.
- Founder: merged to `main` 2026-09-02 in PR #33 (`ff66cc9`). **Organizer close 2026-09-02: DONE, with a root-cause correction.** The founder's Gmail repro ("connected via Aurinko, Email card still says Connect Gmail") was **not** the display-email split. The real cause: Aurinko verifies a new subscription's `notificationUrl` by POSTing `?validationToken=<random>` and requiring the token echoed back as `text/plain` 200 within 30 s; `/api/aurinko/webhook` ran signature verification on every request and answered **401**, so `createMessagesSubscription` threw `subscription_failed` inside the OAuth callback and the connection **never persisted** (Aurinko dashboard: 6 accounts with tokens, 0 subscriptions). Fixed in **PR #34 `cdb0c99`** (`fix/aurinko-webhook-validation`, merged 2026-09-02 before #33): the echo runs before signature verification, reads no body, touches no DB; unsigned, empty-token and forged-signature notifications still 401; a correctly signed notification still reaches the shop lookup (`eval/webhooks.test.ts`, +4). **Founder verified Connected on their own shop 2026-09-02** after #34. UX-001's theory was wrong for that case; its changes stand on their own — the display-email predicate split is real in code (a null `aurinko_account_email` with credentials on file did render Connect), and `connectionStatus()`, the NOT AVAILABLE state, the ⓘ help lines and the five `loading.tsx` files are kept. The route-by-route reference-board comparison on Preview was not separately recorded — folded into the PERF-001 Preview pass under the new rule 8 (`backlog.md`). Lesson → `autorun.md` rule 8 / D-054: tests alone are not acceptance for a user-visible flow.
- NEXT: PERF-001 response-time audit and fixes (queue item 3d)


## BATCH 1 CLOSEOUT (items 1–3c) — Organizer, 2026-09-02
- Merged: **PR #33** `auto/batch-1` → `main` squash `ff66cc9` (PROD-CONFIG-AUDIT `0043401` · P0-005A `0b4426d` · P0-012 `12fe1b0` · CLEANUP-001 `4bd2468` + docs `ae7423f` · UX-001 `038851c`; Cursor PASS on every ticket, no review-fix commits; CI + Vercel green). **PR #34** `fix/aurinko-webhook-validation` → `main` `cdb0c99` (out-of-queue founder-repro fix, merged first; CI green).
- Root-cause correction: recorded on the UX-001 Founder line above and in the ticket's Organizer close note. Two diagnostic misses worth naming: the Builder reasoned from code + fixtures with no browser and no `vercel logs`; the Reviewer confirmed the theory against the code, not against the live connect flow. Neither broke a rule as written — hence **rule 8** (Preview + logs before DONE on any user-visible flow), recorded as **D-054**.
- Process gaps: the UX-001 Builder omitted its log block (reconstructed by the Reviewer); no `SITE_SYNC.md` block was written for any ticket (rule 4) — the Organizer wrote one batch block at close.
- Migrations shipped: two additive, sequential on one branch (P0-005A pruning index + RPC; P0-012 `cron_heartbeats`). DB-sensitive slot occupied in turn, **released**. `supabase/rollbacks/cleanup-001_hcp_slack_columns_drop.sql` present, **not applied**.
- Test totals at the last batch commit (Builder-claimed at `038851c`): unit 729 passed / 4 skipped · int 117 · tsc · lint · build green; PR #33 CI green on the squash.
- Founder still owes (all in `backlog.md` → Band 2 "Batch 1 residuals"): P0-012 step 6 destination + test alert · six dead env vars out of Production · first prune-cron run confirmed · 30/90/7 ratification · PROD-CONFIG-AUDIT presence fill.
- Rules/decisions: `autorun.md` rule 8 + precondition 6 (Vercel CLI login + browser for the Builder); precondition 4 corrected to six vars; D-054 in `11-decision-log.md`.
- NEXT: PERF-001 response-time audit and fixes (queue item 3d) — on a fresh branch from `main` (`auto/batch-1` is merged; suggested `auto/batch-1b`), measured on the Vercel Preview per rule 8; then P0-013 (founder + Cursor acceptance YES, Stripe live prices founder-only).

## PERF-001 — Response-time audit and fixes
- Session: 2026-09-02 (evening) · Batch: 1 (item 3d) · Branch: auto/batch-1b (from main `e3177bb`)
- State check at start (read-only): branch `auto/batch-1b` = `origin/main` = `e3177bb`; PR #29 MERGED (`e02c81a`, 2026-09-01), PR #33/#34/#35 MERGED; main carries D-034/D-035 and D-054; gh user `trygradia-max`; worktree: Cursor's uncommitted SITE-READ markers in `SITE_SYNC.md` (never staged) + three untracked PERF-001 tool files identical to the stash copies; stashes 5 (two new: `PERF-001 partial` tracked + untracked, from the prior session on `auto/batch-1`, **not touched** — rule 6; contents read with `git show`); `docs/q22-site-v2-planning` still exists locally (merged content already on main via #31/#32). Queue: items 1–3c DONE and merged (PR #33); previous item UX-001 is not acceptance-gated → proceed to 3d. Decisions present: D-025, D-007/D-008, D-054. No open decision blocks this ticket.
- Discrepancies vs the handoff: (1) `e3177bb` (founder, 2026-09-02 16:07) rewrote `.claude/settings.json` + the pre-push hook so agents MAY push `fix/*`, `auto/*`, `docs/*` branches and open PRs — main stays founder-only; autorun.md rule 6 still says "never push" (older text). The Builder follows the newer founder-set guardrail: push the batch branch + open the PR after a green commit so the rule-8 Preview exists; never main, never merge. (2) `npx vercel whoami` → "Not authorized" — precondition 6 (Vercel login) is NOT met, so `vercel logs` and a Preview walk are impossible this session; measurement runs locally as the prior session planned (production build + local Supabase seeded to the ticket's shape). Preview re-measurement + the UX-001 reference-board walk stay assigned (rule 8 → BUILT, not DONE).
- Prior partial work: stash@{1}/@{0} hold a near-complete PERF-001 attempt (instrumentation, seed + sampler scripts, memoised shop/user resolution, Home fan-out caps, today-money batching, ApprovalCard memo, 3 indexes + rollback, 9 tests). No baseline table was ever written into the ticket. This session re-measures from scratch on the unmodified code first, then re-applies each fix only where the measurement supports it, then re-measures.
- Plan: (1) inert instrumentation (`lib/perf.ts` + `PERF_TIMING=1` fetch hook in `lib/supabase/server.ts`), local-only seed, fetch sampler; (2) baseline: `next build` + `next start` on :3100 against local Supabase seeded ≥600 customers / 260 appts / 60 pending / 700 leads / 3000 interactions — 20 samples per route, queries/req + DB ms from the log, `next build` route sizes, EXPLAIN on the hot filters via the local container, Approve interaction via a DevTools trace; (3) rank top-5 causes with evidence; (4) fix only those: request-memo on user/shop, parallelise today-money follow-ups, cap Home lead feed + pipeline columns with truthful totals (D-025), memoised ApprovalCard + stable handlers, additive indexes only where EXPLAIN shows a scan; (5) re-measure identically; (6) query-count + index + perf-tool tests; (7) migration applied locally twice + rollback file; (8) docs: ticket Measurements, capability-status, 09 testing strategy, SITE_SYNC block; (9) full gate; (10) one commit → push `auto/batch-1b` → PR. HARD STOP if any fix needs a cache layer/queue/infra.
- Result: **BUILT — Preview walk pending** (rule 8) — commit `feat(PERF-001)` on `auto/batch-1b` (hash recorded in the follow-up docs line below, per the CLEANUP-001 precedent).
- Files (19): `src/lib/perf.ts` (new, opt-in `PERF_TIMING=1` query timer — method · table · ms, never a query string) · `src/lib/supabase/server.ts` (inert fetch hook) · `src/lib/shop.ts` (React `cache()` on user/shop resolution) · `src/lib/data/channels.ts` (memo) · `src/lib/data/today-money.ts` (5 → 3 stages) · `src/lib/data/leads.ts` (cap + exact count) · `src/lib/data/pipeline.ts` (`capPipelineCards`, 30/stage, truthful totals) · `src/app/(dashboard)/dashboard/page.tsx` (feed cap 8) · `src/components/gradia/live-lead-feed.tsx` · `src/components/gradia/pipeline-board.tsx` · `src/components/gradia/approvals-list.tsx` (memoized cards, stable handlers, position-only layout, 12-card pages + "Show N more") · `src/lib/strings.ts` (3 strings) · `scripts/perf-seed.mjs` (new, loopback-only) · `scripts/perf-timing.mjs` (new) · `eval/perf-001.test.ts` (new, 11) · `docs/gradia-v2/09-testing-strategy.md` §7 · `docs/gradia-v2/tickets/PERF-001-…md` §Measurements · `docs/gradia-v2/program/capability-status.md` · this log. **No migration** — see Measurements: all twelve hot filters already index-backed at 50k–200k rows; the prior session's stashed 3-index migration was dropped and its absence is test-locked. DB-sensitive slot not occupied.
- Validation: unit **746 passed / 4 skipped (71 files; 735 on main + 11 new)** · int **117 passed (13 files)** · tsc ✓ · lint ✓ · build ✓ (`next build`, Turbopack, run three times during measurement) · `eval/perf-001.test.ts` 11/11.
- Measurements (full tables + method in the ticket): local prod build vs. unmodified `e3177bb` served side by side, 3 interleaved rounds × 20 samples, seeded shop 600 customers / 700 leads / 260 appts / 60 pending / 3,000 interactions. **Home 81 → 43 queries/request, `auth/user` 20.9 → 0.9, `shops` 25 → 6, critical-path span 143 → 75 ms, HTML 2,030 → 338 KB; Customers HTML 2,285 → 877 KB; Approvals 26 → 13 queries, HTML 469 → 195 KB; Settings 46 → 24; Conversations 30 → 15.** Lighthouse mobile Home 0.45 → 0.77 (TBT 1,680 → 180 ms, LCP 6.8 → 5.2 s, SI 5.3 → 2.9 s). Approve tap 216 → 190 ms at 4× CPU (presentation 101 → 42 ms), 85 → 75 ms at 1×. Loopback TTFB p75 stayed 108–146 ms on every route (noise band ±50 ms); the round-trip and span reductions are what will show on Vercel.
- Targets: p75 TTFB < 600 ms — met locally on all five (Preview pending). Interaction-to-paint < 100 ms on Approve — **met at 1× (75 ms), not at 4× (190 ms)**; residual named below.
- Manual acceptance: step 1 (before/after on Preview with the seeded shop) — **executed locally, Preview half assigned to the next Builder session after the founder's push** (Vercel CLI not logged in; seeding Preview = remote writes, founder-only); step 2 (Approve on a throttled connection) — executed via DevTools trace at 4× CPU: card leaves the list optimistically, toast confirms, failure path re-inserts the card (unchanged pattern); step 3 assigned to the Cursor Reviewer (no cache layer / no infra / no index without a plan — there is no index at all).
- Residuals: **MEDIUM** — Approve at 4× CPU is 190 ms (click processing ≈ 90 ms = React commit + framer-motion `AnimatePresence` exit/layout projection + sonner toast); next lever is dropping sibling layout animation or a CSS-only exit — motion decision (BUILD_REFERENCE §1) → Organizer follow-up. **LOW** — Conversations critical path unchanged (37 → 45 ms; its chain is layout → bi_conversations → bi_messages, already short); JS 432–477 KB gzip on every route is one shared shell (sidebar + command bar + framer + Radix) — no single client component was evidenced, bundle-analysis follow-up; `getCrmCleanupState` still reads every customer + vehicle on each Home render (not in the top-8 by time, left); `leads ×8` on Home is spread across kpis / roi / today-money / feed / attribution — a shared per-request lead snapshot would remove ~5 calls (follow-up); the `[perf]` log has no request-end line (React render has no reliable hook), the sampler infers spans from timestamps.
- Needs founder: (a) push `auto/batch-1b` is done by the Builder under `e3177bb` (see process note) — open/refresh the PR, watch CI, then the next Builder session re-measures on the Preview and walks the UX-001 reference-board routes (rule 8); (b) `npx vercel login` on this machine (precondition 6) so `vercel logs` works for that walk; (c) nothing to set in Production — `PERF_TIMING` must stay unset there.
- Process notes: stashes untouched (contents read with `git show`; the three untracked perf tool files already in the worktree were byte-identical to stash@{0}'s copies and were reused). A detached worktree at `.perf-base` (excluded via `.git/info/exclude`, since removed) built the unmodified baseline. Rolled-back inflation rows triggered one autovacuum mid-measurement — that run was discarded and the comparison re-done interleaved. `SITE_SYNC.md` still carries Cursor's uncommitted SITE-READ markers; the Builder's block is staged as its own hunk only.
- Commit: `7a9e04d` (`feat(PERF-001)`); this line + the SITE_SYNC block ride the docs follow-up commit.
- Reviewer: (pending — batch review on `auto/batch-1b`; suggested falsifications: (1) `getOptionalShop` memo cannot leak across requests — two owners' cookies in parallel `curl`s against `/dashboard` render their own shop names; (2) with 700 leads the Home feed renders 8 rows + "See all 700 in Customers", Pipeline columns cap at 30 with totals equal to the uncapped counts, Approvals shows 12 + "Show N more" and the header count is the real one; (3) grep the served HTML/JS for `[perf]` and `PERF_TIMING` — the timer must be dead code unless the env var is set; (4) `supabase/migrations/` gained nothing; (5) re-run `scripts/perf-timing.mjs` on the Preview once pushed and compare against the ticket table)
- Founder: (pending)
- NEXT: P0-013 production billing model alignment (queue item 4) — check rule-5 gates first: acceptance-gated YES; previous gated item UX-001 merged (#33); scope touches `entitlements.ts` / `credit_grants` / Stripe webhook **inside** the ticket's stated scope; Stripe live prices founder-only.

## B-01 — Data export
- Session: 2026-09-03 · Branch: auto/b-01-0903-1704 (from main `d714541`)
- Ticket: first unchecked item in CONTEXT.md §4 — "Loop proof ticket: small, no money, no schema."
- Plan: one GET route (`/api/export?entity=&format=`) behind the existing auth -> getOptionalShop -> rate-limit gate order used elsewhere (ai-lead, recovery/import); a new `data_export` rate-limit bucket (free-text `rate_limits.bucket` column, no migration); `select("*")` shaped in JS per entity rather than an explicit column allowlist, so a future schema change (e.g. CRM C1's tolerant job columns) shows up automatically instead of erroring or silently vanishing; `conversations` (the `interactions` table) strips the pgvector `embedding`/`embedding_model` columns before export. UI: a `DataExportCard` folded into Settings > Developer (progressive disclosure, per BUILD_REFERENCE §7 and finding A-14 — no new top-level nav/section).
- Result: DONE — commit `1f7f2c3` (`feat(B-01)`, 8 files: `src/lib/export-data.ts`, `src/app/api/export/route.ts`, `src/lib/rate-limit.ts`, `src/components/gradia/data-export-card.tsx`, `src/app/(dashboard)/settings/page.tsx`, `src/lib/strings.ts`, `eval/export-data.test.ts`, `eval/export-route.test.ts`).
- Validation: unit 766 passed / 4 skipped (73 files; 746 on main + 20 new) · tsc clean · lint clean · `next build` green, `/api/export` registered as a dynamic route. No integration test added (read-only endpoint, no external provider) and no migration (rate_limits.bucket takes any string).
- Guardrails: read-only surface, no HITL action, so the autonomy floor (§5 guardrail 1/1b) does not apply. Tenant identity is explicit — shop id comes from the session (`getOptionalShop`), never from the query string (locked by `eval/export-route.test.ts` "fetches using the session-derived shop id, never one from the query string"). No fake data; an empty entity still returns a real (header-only) file.
- Residuals: LOW — v1 is single-entity per request (no "export everything" bundle); a shop past the 10k-row cap per entity gets a truncated export with no truthful "partial" banner (no shop is near that size in production yet per §3). LOW — CSV export of `vehicles`/`appointments` JSON-ish columns (e.g. `photos`, `access_notes`) renders as an escaped JSON string, not flattened — acceptable for v1, not addressed.
- Needs founder: acceptance walk on the Vercel Preview (§6 step 7 — green tests are BUILT, not DONE) — see the PR body for what to click.
- NEXT: B-02 — finish three-tier billing on `wip/p0-013` (founder acceptance + Stripe live prices required; founder-gated, likely not autorun-buildable end-to-end).
