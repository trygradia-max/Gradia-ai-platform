# Program — Current Sprint

_Created 2026-07-25 by the Organizer. Sprint 1 (P0 Stabilization). Started 2026-07-25 · target: alpha 2026-08-07 (**date-vs-gate policy open — Q-25 in `decision-queue.md`**; do not silently relax the P0 exit gate). Governs live sprint state; tickets in `../tickets/`, limits also mirrored in `work-in-progress.md`._

## Sprint goal

Land the two tickets that everything else depends on — **P0-001** (the leaked database credential is rotated and scrubbed from the working tree) and **P0-002** (CI can actually stop a broken build from reaching `main` = production). Nothing from P1+ enters this sprint. Sprint 1 contains **only P0 stabilization work** (epic E00).

Rationale: audit doc 00 names the credential leak the single most serious issue; audit doc 12 orders it first ("nothing else ships until 1–2 land"). P0-002 must complete before any other ticket enters review, because reviewers need a CI that can fail.

## Active implementation tickets

| Ticket | Title | Status | Risk class | Builder | Reviewer |
|---|---|---|---|---|---|
| P0-001 | Exposed database credential remediation | **in-review** (accuracy update 2026-08-06: **merged to `main` 2026-07-30** in PR #8, commit `6adc21c` — the credential is out of HEAD and the regression lock is live in CI; still held out of done pending founder acceptance steps 2 & 6 and formal Reviewer sign-off; Q-01 history decision open) | **Security (standard)** — reclassified 2026-07-27: the ticket declares no schema/migration impact, so it does NOT occupy the DB-sensitive slot (WIP definition = tables/indexes/RLS/migrations) | Claude Builder (session 2026-07-29) | _unassigned — one Cursor Reviewer_ |
| P0-002 | CI typecheck, lint, build and integration enforcement | **done** (2026-07-30 — merged to `main` in PR #9, commit `7e3d530`; Cursor Reviewer verdict APPROVE, no BLOCKER/HIGH findings; completion record in the ticket file) | Standard | Claude Builder | Cursor Reviewer (APPROVE) |

Founder action inside P0-001: the actual password rotation happens in the Supabase dashboard — only the founder can do it. The Builder prepares everything around it (scrub `.gitignore:46`, env updates, delete `.env.local` backups, verification steps). The git-history-scrub sub-step is **blocked** on decision Q-01 (see `blocked.md`) — rotation is NOT blocked by it.

## Queued behind this sprint

- **P0-003** (Central appointment conflict service) — **done 2026-08-06**: merged to `main` in PR #10 (`00091db`), Cursor Reviewer APPROVE, CI + DB integration green. The service (`src/lib/availability.ts`) is **inert until P0-004 wires it** — no booking path changed, no migrations. Merge/review record in the ticket file.
- **P0-004** (Conflict enforcement across booking and scheduling paths) — **done 2026-08-11**: merged to `main` in PR #12 (`3b6d044`), CI + DB integration green; independent Cursor final review **merge APPROVE · production enablement NOT READY** (verdict supplied to the Founder outside the GitHub PR trail — the PR carries no review artifact); fix rounds `d43ce16` (Cursor) and `c0b66b1` (founder rollout/failure-policy) on-branch. Enforcement is **dormant in production** (`NEXT_PUBLIC_GRADIA_CONFLICT_ENFORCEMENT` unset = OFF); production enablement is a separate gate: founder manual QA (steps 1–7) on a flag-on Preview **plus** the P0-004A hardening below. Merge/review record in the ticket file.
- **P0-004A** (Appointment booking atomicity and concurrency) — **done 2026-08-11**: merged to `main` in PR #15 (`2103943`; pre-squash `522203f` Builder → `31f410a` Cursor fix); `ci / checks` + `ci-integration / integration` green including real-Postgres concurrency tests (one transient upstream Supabase/PostgREST fixture failure; rerun green); independent Cursor verdict **APPROVE** — one BLOCKER (serialized overlap refusal active with enforcement OFF) found and fixed pre-merge via `p_enforce_conflicts`. Closes the booking false-executed, duplicate/replay, partial-ordering, and check→insert concurrency gaps; advisory locking + `pending_action_id` idempotency stay active even with enforcement OFF. Production conflict enforcement remains OFF. Completion record: `../tickets/P0-004A-appointment-booking-atomicity-concurrency.md`.
- **P0-005** (Webhook event idempotency foundation) — **done 2026-08-13**: merged to `main` in PR #17 (`e1dedfb`; pre-squash `dec4c38` Builder → `ec28a5a` ADR docs/review); independent Cursor verdict **APPROVE**, no BLOCKER or HIGH code defects (ADR-001 C1); founder production duplicate audit **zero rows** on both ledgers (C7); retention follow-up **P0-005A** filed (C2). Durable `provider_events` claiming + ledger uniques + SELECT-only ledger RLS live; P0-006/007 route wiring deliberately not started. Staging manual acceptance still gates full rollout acceptance of the migrations. Close record: `../tickets/P0-005-webhook-idempotency-foundation.md`.
- **P0-005A** (provider_events retention and pruning) — **filed 2026-08-13** per ADR-001 C2 (`../tickets/P0-005A-provider-events-retention-pruning.md`); ready, unscheduled — must land before P0-006/007 receipt volume makes the claim table operationally significant.
- **P0-006** (Twilio inbound replay protection) — **done 2026-08-14**: merged to `main` in PR #19 (`76847e4`; pre-squash `afb542b` Builder → `89af55c` metering retry-safety fix — a real reliability bug CI exposed, not a review fix); CI green (`ci / checks` + `ci-integration / integration` + Vercel/Preview); independent Cursor verdict **APPROVE / safe to merge**, no BLOCKER or HIGH findings, no review-fix commit required; founder real-Twilio staging acceptance completed pre-merge (valid inbound, MessageSid replay, STOP flow + STOP-SID replay, invalid-signature rejection). Twilio inbound SMS route now claims `provider_events` strictly after signature verification (ADR-001 C3 satisfied for this route); no new migration; status/A2P callbacks remain P0-008. One accepted residual: a provider retry after a genuine metering-write failure may re-run the classifier (non-blocking; optional optimization). Close record: `../tickets/P0-006-twilio-inbound-replay-protection.md`.
- **P0-007** (Vapi transcript and usage replay protection) — **done 2026-08-14**: merged to `main` in PR #21 (`8a4d4d1`); independent Cursor verdict **APPROVE**, no BLOCKER or HIGH findings, no review-fix commit required; founder acceptance **PASSED** on isolated local staging (first delivery, ×2 replay, post-restart durability replay >300s after completion, financial reconciliation with zero drift, production `VAPI_DEFAULT_SHOP_ID` fallback-guard refusal, global reconciliation — zero duplicates everywhere). End-of-call report now claims `provider_events` (`provider='vapi'`, `event_id=call.id`) strictly after authentication (ADR-001 C3); route `maxDuration=60` under the 300s stale threshold (ADR-001 C5); transcript replay idempotent; `voice_minute` metering protected by the claim + durable `usage_events` unique with retryable/fail-closed `recordUsage`; production fallback fails closed for unmatched assistants; **no new migration** (the P0-005 schema sufficed — the slotting reconciliation resolved as ADR-001 expected). Accepted residuals (cross-tenant call-id griefing, tool-call replay out of scope, best-effort `call_records`, count-based transcript resume, `maxDuration` headroom) + follow-ups recorded in the ticket close record and `backlog.md`. Close record: `../tickets/P0-007-vapi-transcript-usage-replay-protection.md`.
- **P0-008** (Twilio subaccount status callback repair) — **done 2026-08-25**: merged to `main` in PR #23 (`1ea198f`; pre-squash `ffd6e01` Builder, implemented 2026-08-18); `ci / checks` + `ci-integration / integration` + Vercel Preview green on the exact reviewed commit; independent Cursor verdict **APPROVE**, no BLOCKER or HIGH findings; founder acceptance **PASSED** on isolated local staging (mock Twilio REST via the `TWILIO_API_BASE` seam; real staged `send_sms` → `executeApproval` path for both a Gradia-subaccount shop and a BYO shop; `credentialSource=subaccount`/`byo` observed; forged/missing-signature 401s, tampered-`?shop=` 401, unknown shop 404 with no credential fallback, unknown MessageSid 200 no-op, cross-tenant own-token scoped no-op, replay durable, reconciliation clean). Status route now resolves credentials subaccount → BYO → env master like inbound, rejects unknown `?shop=` instead of falling through to env-master, and tenant-scopes the interaction lookup/update. No migration; no provider_events claim (naturally idempotent last-write-wins, test-asserted); A2P route audited unchanged. Residuals M1/L1/L2/L3/L4 dispositioned in the close record; follow-ups in `backlog.md`. Close record: `../tickets/P0-008-twilio-subaccount-status-callback-repair.md`.
- **P0-009** (Quote acceptance, lead linkage and expiration repair) — **done 2026-08-26**: merged to `main` in PR #25 (`d3c0e4d`; pre-squash `829ddfd` Builder → `aba1068` Cursor review-fix); `ci / checks` + `ci-integration / integration` + Vercel Preview green on the exact reviewed commit `aba1068`; independent Cursor verdict **APPROVE**, no BLOCKER, **one HIGH found and fixed pre-merge** (transient quote-lead read errors could fall through to fallback lead creation — now fails closed with a `lead_resolve_error` reconciliation marker and safe replay); founder acceptance **PASS 2026-08-26** on isolated local staging (accept→book reusing the original quote-linked lead with one card and one `booked` transition, expired-quote refusal + replay refusal, honest `accepted_no_booking` no-phone state, shop-keyed rate limiting, voice-regression fallback intact, read-error fail-closed + replay heal, tenant isolation, clean reconciliation). Quote advances to `booked` only after durable appointment persistence; one additive migration (`quote_status = booked`); P0-004/P0-004A booking architecture preserved. Q-04 (richer expired-quote UX) stays open non-blocking; token regeneration stays deferred; residuals M-1/L-1/L-2/L-3 dispositioned in the close record; follow-ups in `backlog.md`. Close record: `../tickets/P0-009-quote-acceptance-lead-linkage-expiration.md`.
- **P0-010** (Production environment and error-surface cleanup) — **done 2026-08-28**: merged to `main` in PR #27 (`5d82fa3`; reviewed/accepted tree `618cf41` = Builder `aea2d41` → Cursor review-fix `618cf41`); `ci / checks` + `ci-integration / integration` + Vercel Preview green on the exact reviewed commit; independent Cursor verdict **APPROVE**, **one HIGH found and fixed pre-merge** (AI-lead extraction consumed 1 shop credit because `priceUsage()` rounds the zero-retail `inbound_classify` SKU up to 1 — now passes `credits: 0` explicitly, test-locked); founder acceptance **PASS 2026-08-28** on isolated local staging (unauthenticated AI-lead replay refused with zero writes; authenticated extraction succeeded; tenant isolation; inactive-plan + exhausted-credit refusals pre-model; `inbound_classify` credits=0 with balance unchanged; provider-failure no-meter; `ai_lead` 20/60s per-shop rate limit; dashboard error boundary/not-found/loading surfaces; revalidation + copy truth; env-backup hygiene; orphan-deletion regression; P0-007 Vapi fail-closed regression). **Production billing exception recorded as PASS behavior:** `STRIPE_PRICE_*` env vars intentionally absent from Production — checkout proven fail-closed before any Stripe call — until **P0-013 — Production billing model alignment** (draft, decision-gated on Q-22, **launch-blocking before live paid billing activation**) is implemented, reviewed, accepted, and ready. Residuals M-1/M-2/M-3 recorded not repaired; M-4 satisfied by this closeout. Close record: `../tickets/P0-010-production-env-error-surface-cleanup.md`.
- **P0-011** (Service-role tenant-scoping review and helper design) — **done 2026-09-01**: merged to `main` in PR #29 (squash `e02c81a`; Builder `34c83fa` → Cursor review-fix accepted tree `3446fe2`); `ci / checks` + `ci-integration / integration` + Vercel green on the exact accepted SHA; independent Cursor verdict **APPROVE AFTER LOCAL FIX** — **two HIGH found and fixed pre-merge** (HIGH #1: `forShop.update()` scoped WHERE but didn't stamp SET, so a forged payload `shop_id` could attempt to re-tenant a row — update now stamps the authorized shop; HIGH #2: Stripe **Connect** `checkout.session.completed` events, carrying attacker-controllable `client_reference_id`/`metadata.shop_id`, could enter the platform billing handler — Connect events are now rejected there before any `shops`/`credit_grants` mutation); founder acceptance **PASS** on `3446fe2` (660 unit / 109 integration / tsc / lint / build; cross-tenant claim refusals with `TENANT_SCOPE_VIOLATION` structured logging and zero writes, re-tenant attack defeated, forged-Connect billing no-op with legitimate checkout intact, strict agent-config zod incl. forged-`shop_id` rejection, L-1 scoping verified both directions, Slack structurally dormant + posted-card binding, full P0-005…P0-010 regression matrix PASS). Closes audit **C-2** (claim tenant binding + Slack), **L-1**, **M-2**; **L-2** verified already fixed by P0-009. Full 31-file sweep table + **ADR-003 founder-APPROVED** (forShop direction; TS-1…TS-6 stay future) in the close record: `../tickets/P0-011-service-role-tenant-scoping-review.md`. No migration. One test-infra note: a P0-009 rate-limit fixed-window timing flake surfaced once locally during acceptance (passed in isolation, on rerun, and in CI) — tracked as flaky-test hygiene in `backlog.md`, not a P0-011 regression.
- **P0-012** (Monitoring alert delivery and incident hooks) — promoted to the **next implementation position** at the P0-011 close (2026-09-01), per backlog order; **ready** (the delivery seam builds now; only the final Q-08 destination config waits, per `blocked.md`). Queued consumers: the P0-011 `TENANT_SCOPE_VIOLATION` signal and the P0-008 M1 credential-observability follow-up. Enters in-progress only when the Organizer slots it and a Builder is recorded on the WIP board — not started.
- **P0-013** (Production billing model alignment) — cut 2026-08-28 as **draft — decision-gated** on the founder's Q-22 tier decisions; launch-blocking before live paid billing activation (entry in `blocked.md`; ticket file in `../tickets/`). Not started.

## WIP limits (restated, binding)

| Limit | Rule | Current state |
|---|---|---|
| Active implementation tickets | max **2** | 1 active (P0-001 in-review); P0-002/P0-003/P0-004/P0-004A/P0-005/P0-006/P0-007/P0-008/P0-009/P0-010/P0-011 done (latest: P0-011, 2026-09-01, PR #29) — one slot free, reserved next for P0-012 (ready — next implementation position) ✓ |
| Database-sensitive tickets active | max **1** | **0** — P0-009 shipped **one additive migration** (`quote_status = booked` — its spec allowed "at most one additive"; the slot was occupied during its run and released at the 2026-08-26 close); slot free ✓ |
| Payment / tenancy / calendar high-risk tickets active | max **1** | 0 — P0-007 occupied the payment/metering slot during its run and released it at close (2026-08-14); free again ✓ |
| Roles per ticket | exactly one Builder + one Reviewer | slots defined above ✓ |
| Entry condition | no ticket enters implementation until dependencies and decisions are resolved | P0-001/P0-002 have no unresolved blockers (Q-01 blocks only the optional history-scrub sub-step) ✓ |

## Definition of sprint success

1. P0-001 done: old credential fails to connect; `.gitignore:46` line gone from HEAD; backup env files deleted; rotation documented (per the ticket's DoD).
2. P0-002 done: a deliberate type error fails CI; lint + `next build` run on every push; `ci-integration.yml` runs green without `continue-on-error`. — **MET 2026-07-30** (PR #9; secret hygiene, typecheck, lint, deterministic tests, build, and DB integration all blocking; GitHub requires `ci / checks` + `ci-integration / integration` on `main`; evidence in the ticket's completion record).
3. Both pass Cursor Reviewer sign-off against `12-definition-of-done.md`.
4. `blocked.md` and `decision-queue.md` updated with anything discovered mid-sprint.

## Carry-over rules

- An unfinished ticket carries into Sprint 2 **keeping its slot** — it is not re-scoped mid-flight.
- A ticket that fails review returns to in-progress under the same Builder; it does not free a WIP slot.
- If a founder decision blocks a ticket mid-sprint, the ticket moves to `blocked.md` and its slot frees; the Organizer may promote the next ready ticket that respects the limits.
- Scope discovered mid-ticket goes to `backlog.md` as a follow-up ticket — never absorbed silently.
