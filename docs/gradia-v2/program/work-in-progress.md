# Program — Work In Progress

_Created 2026-07-25 by the Organizer. The live WIP board. **Every Builder session checks this file before starting any work** — that is an invariant, not a courtesy. The Organizer is the only role that edits it._

## WIP limits (binding, from `../README.md`)

1. Maximum **two** active implementation tickets at any moment.
2. Maximum **one** database-sensitive ticket active at a time (anything adding/altering tables, indexes, RLS, or migrations).
3. Maximum **one** payment, tenancy, or calendar **high-risk** ticket active at a time — one slot shared across all three categories.
4. Exactly **one Builder and one Reviewer per ticket** — never two builders on one ticket, never one session in both roles.
5. **No ticket enters implementation until its dependencies and decisions are resolved** (`dependency-map.md`, `decision-queue.md`, `blocked.md`).

## Current board (Sprint 1, as of 2026-07-25)

| Slot | Ticket | Risk class | Builder | Reviewer | State |
|---|---|---|---|---|---|
| Active 1 | P0-001 — Exposed database credential remediation | Security (standard) — reclassified 2026-07-27, no schema/migration impact | Claude Builder (session 2026-07-29) | _open — assign one Cursor Reviewer_ | **in-review** (accuracy update 2026-08-06: merged to `main` 2026-07-30, PR #8 `6adc21c`; held out of done pending founder acceptance steps 2 & 6 + formal Reviewer sign-off; slot stays occupied until done) |
| Active 2 | — freed 2026-08-26 — | — | — | — | **P0-009 done** (merged PR #25 `d3c0e4d` 2026-08-26; pre-squash `829ddfd` Builder → `aba1068` Cursor review-fix, both 2026-08-25; independent Cursor verdict **APPROVE**, no BLOCKER, one HIGH found and fixed pre-merge in `aba1068`; founder acceptance **PASS 2026-08-26** on the exact reviewed commit — accept→book lead reuse, expiry refusal + replay, honest no-phone state, rate limiting, voice regression, read-error fail-closed + replay heal, tenant isolation, clean reconciliation; close record in `../tickets/P0-009-quote-acceptance-lead-linkage-expiration.md`. Board note: like P0-004 through P0-008, this ticket ran founder-slotted without a board entry recorded at start — recorded here retroactively for accuracy; it entered after the `docs/close-p0-008` closeout landed as `eae12a5` PR #24, exactly per its unblock condition). Previous occupant P0-008 done 2026-08-25 (PR #23, close record in its ticket file). **P0-010 done 2026-08-28** (merged PR #27 `5d82fa3`; reviewed/accepted tree `618cf41` = Builder `aea2d41` → Cursor review-fix `618cf41`; independent Cursor verdict **APPROVE**, one HIGH — AI-lead consuming 1 credit via `priceUsage()` zero-retail rounding — found and fixed pre-merge; founder acceptance **PASS 2026-08-28** on the exact reviewed commit: M-1 auth/tenant/gate/rate-limit matrix, credits=0 metering with balance unchanged, error/not-found/loading surfaces, revalidation + copy truth, env-backup hygiene, orphan-deletion regression, P0-007 Vapi fail-closed regression, and the **recorded production billing exception** — `STRIPE_PRICE_*` intentionally absent from Production, checkout proven fail-closed before any Stripe call until **P0-013** lands; close record in `../tickets/P0-010-production-env-error-surface-cleanup.md`. Board note: like P0-004 through P0-009, it ran founder-slotted without a board entry at start — recorded retroactively; its unblock condition, the `docs/close-p0-009` closeout, had landed as `e70b287` PR #26.) **P0-011 done 2026-09-01** (merged PR #29 squash `e02c81a`; Builder `34c83fa` 2026-08-28 → Cursor review-fix accepted tree `3446fe2`; independent Cursor verdict **APPROVE AFTER LOCAL FIX** — two HIGH found and fixed pre-merge: forShop.update now stamps the authorized shop_id so a forged payload cannot re-tenant a row, and Stripe Connect events are rejected from the platform billing handler so attacker-minted Checkout metadata cannot write another shop's plan/subscription/grants; founder acceptance **PASS** on the exact accepted SHA — cross-tenant claim refusal with `TENANT_SCOPE_VIOLATION` logging and zero writes, re-tenant attack defeated, forged-Connect billing no-op with legitimate checkout intact, strict agent-config validation, L-1 delete/revoke scoping, Slack structurally dormant + posted-card binding, 31-file inventory locked, P0-005…P0-010 regressions all PASS; **ADR-003 founder-APPROVED**, TS-1…TS-6 remain future; no migration; close record in `../tickets/P0-011-service-role-tenant-scoping-review.md`. Board note: like P0-004 through P0-010, it ran founder-slotted without a board entry at start — recorded retroactively.) Slot reserved next for **P0-012** (Monitoring alert delivery and incident hooks — promoted to next implementation position at this close per backlog order; **ready**: the delivery seam builds now, only the Q-08 destination config waits per `blocked.md`; consumers queued for it include the new `TENANT_SCOPE_VIOLATION` signal and the P0-008 M1 credential-observability follow-up), which enters in-progress only when the Organizer slots it and a Builder is recorded here — not started. **Superseded by autorun (D-037): Batch 1 items 1–3c ran on `auto/batch-1` and are done 2026-09-02** — PR #33 squash `ff66cc9` (PROD-CONFIG-AUDIT `0043401` · P0-005A `0b4426d` · P0-012 `12fe1b0` · CLEANUP-001 `4bd2468` + `ae7423f` · UX-001 `038851c`; Cursor PASS on each; founder merged; none acceptance-gated) plus the out-of-queue Gmail fix PR #34 `cdb0c99`. They ran without per-ticket board entries by design (`autorun.md` batches the lane model — this row's reservation covered the batch). Slot reserved next for **PERF-001** (queue item 3d, fresh branch from `main`), then **P0-013** (payments + DB — founder + Cursor acceptance). |
| Database-sensitive | — empty — | — | — | — | Slot free. **Accuracy correction at the 2026-08-26 close:** P0-009 shipped **one additive migration** (`20260825120000_quote_status_booked.sql`, adds `quote_status = booked`) — its spec allowed "at most one additive, idempotent migration", so the earlier "expects none (confirm at slotting)" note resolved to *one*; the slot counts as occupied during its run and released at close. P0-008/P0-007/P0-006 shipped none; P0-005's migrations closed 2026-08-13. P0-005A expects no migration (confirm at slotting); P0-010 shipped none (confirmed at the 2026-08-28 close); P0-011 shipped none (confirmed at the 2026-09-01 close); P0-012 expects none (confirm at slotting); P0-013 will occupy this slot when it eventually enters (one additive tier migration — decision-gated on Q-22). **2026-09-02 Batch-1 close:** P0-005A (`20260901120000_provider_events_pruning.sql` — partial index + service-role RPC) and P0-012 (`20260901130000_cron_heartbeats.sql`) each shipped **one additive migration** on `auto/batch-1`, sequentially — the slot was occupied in turn and is **released**. Q-22 is resolved (D-034/D-035); P0-013 enters after PERF-001 (which takes the slot only if it adds indexes). |
| High-risk (payments/tenancy/calendar) | — empty — | — | — | — | Free. P0-009 was money-path correctness (quotes) but staged no payments and touched no payment rails — ruled **not** high-risk class at close (retroactive, consistent with its spec: no deposits/payments; money+calendar writes stayed HITL); it never occupied this slot. Free since the P0-007 (payment/metering) close, 2026-08-14. P0-010 was not high-risk class (confirmed at its 2026-08-28 close). **P0-011 (tenancy class) occupied this slot during its run and released it at the 2026-09-01 close.** P0-012 is not high-risk class; P0-013 is **payments** class (decision-gated on Q-22) and will occupy this slot when it eventually enters. |

Slot accounting: 1/2 active · 0/1 database-sensitive · 0/1 high-risk. One active slot free — reserved for PERF-001 (autorun Batch 1 item 3d; items 1–3c done 2026-09-02, PR #33). DB-sensitive slot released at the 2026-09-02 Batch-1 close.

## How this board is updated

- **Start:** the Organizer moves a ticket from `current-sprint.md`/`backlog.md` into a slot only when limits allow and entry conditions pass; the ticket file's Status flips to in-progress.
- **Finish:** Builder posts the completion report (per `../agent-briefs/claude-builder.md`) → Reviewer signs off against `../12-definition-of-done.md` → Organizer clears the slot and flips the ticket to done.
- **Block:** a blocked ticket moves to `blocked.md` and frees its slot.
- **Never:** a Builder self-assigns, swaps tickets mid-flight, or starts "just a small extra fix" outside a slotted ticket. Out-of-scope discoveries go to `backlog.md` via the completion report.
